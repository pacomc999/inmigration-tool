---
phase: 03-cheat-sheet-rendering-print-mobile-onboarding-privacy
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01-PLAN.md
files_modified:
  - index.html
  - .planning/REQUIREMENTS.md
  - README.md
autonomous: true
requirements:
  - TRUST-03
  - TRUST-05
tags: [onboarding, privacy, trust-05-housekeeping, anthropic-link, appstate-screen, phase-3]
decisions:
  - "Sequenced AFTER Plan 03-02 (Wave 3) because both 03-02 and 03-03 modify `index.html`. Same-wave plans must have zero `files_modified` overlap; we serialize. Plan 03-03 is logically independent of Plan 03-02 (no DOM dependency beyond the Plan 03-01 cheat-sheet that ships in Wave 1)."
  - "Two new screens added to the locked appState screen union: `'onboarding'` and `'privacy'`. Updated union: `'key-entry' | 'intake' | 'generating' | 'result' | 'error' | 'onboarding' | 'privacy'`. The render() switch grows two new cases. hideAllSections sweep is extended to include the two new `<section>` ids."
  - "Onboarding trigger: on DOMContentLoaded, after the key load, before deciding the initial screen, check `localStorage.getItem('migrationsamt.onboardingSeen')`. If absent or not equal to `'true'`, route to `'onboarding'`. Otherwise proceed with the existing logic (key present -> `'intake'`, key absent -> `'key-entry'`). Per Section 6."
  - "Storage key: `migrationsamt.onboardingSeen`, matching the existing `migrationsamt.consentGiven` prefix. New module-level const `ONBOARDING_STORAGE_KEY` next to `CONSENT_STORAGE_KEY` (around index.html line 768)."
  - "Privacy navigation: `appState.returnTo` is a NEW field holding the screen value the user was on when they clicked Privacy. Privacy's Back button reads `returnTo` (default `'intake'`) and clears it. Per Section 7."
  - "Privacy link rendered in the page footer on every non-result screen AND inside `.cheat-sheet-controls` on the result screen (the result screen footer is hidden by Plan 03-02's print stylesheet, so a result-screen Privacy link in the controls bar is the way to reach Privacy from the cheat sheet). Per Section 7 + Section 4 Open Question 2."
  - "Onboarding body copy: Section 6 verbatim. Links to `https://console.anthropic.com` (base console, NOT `/settings/keys`) so unsigned-up users see the sign-up flow first. Per Section 6 / Section 11."
  - "Privacy body copy: Section 7 verbatim. Includes: what stays in browser (key, intake, sheet, two boolean flags), what's sent over network (single Anthropic request), what is NEVER done (no backend, no analytics, no third-party scripts, no microphone EVER, no cookies), and the right to clear."
  - "TRUST-05 housekeeping: amend `.planning/REQUIREMENTS.md` line 49 from `A first-visit onboarding screen explains how to get a free Gemini API key, with link` to `A first-visit onboarding screen explains how to get an Anthropic API key, with link`. Drop the word `free` because Anthropic is paid pay-as-you-go (very cheap, but not free). Per Section 11. No other REQUIREMENTS edits."
  - "README.md update: add a short `First-visit onboarding` section describing the new screen + the Anthropic Console link. Do NOT change any existing Phase 1 / Phase 2 sections."
  - "Single `index.html` file with inline `<style>` and `<script>` (L-05). All new DOM, CSS, and JS live inside the existing file. No new external assets."
  - "All copy uses ASCII hyphens. No em or en dashes (CLAUDE.md project rule)."
  - "Anthropic Console URL stored as a module-level constant `ANTHROPIC_CONSOLE_URL = 'https://console.anthropic.com'` near the other constants so link text and href stay in sync if Anthropic ever moves the URL."

must_haves:
  truths:
    - "On a fresh browser profile with no localStorage keys, opening `index.html` lands on the new `'onboarding'` screen (not on `'key-entry'`)"
    - "Onboarding screen shows the verbatim body copy from 03-RESEARCH.md Section 6, including the `Get an Anthropic API key` link pointing to `https://console.anthropic.com`"
    - "Clicking `Got it, continue` on onboarding writes `migrationsamt.onboardingSeen = 'true'` to localStorage and routes to `'intake'` if a key is saved, else `'key-entry'`"
    - "Reloading the page after dismissing onboarding does NOT show onboarding again"
    - "Clearing localStorage (e.g. DevTools -> Application -> Storage -> Clear) makes onboarding show again on next load"
    - "A `Privacy` link is visible on `'key-entry'`, `'intake'`, `'onboarding'`, and `'error'` (page footer or header strip)"
    - "A `Privacy` link is also visible in `.cheat-sheet-controls` on the result screen (because the page `<footer>` is hidden by Plan 03-02's print stylesheet)"
    - "Clicking the Privacy link routes to `'privacy'`, displays the Section 7 body copy verbatim, and shows a Back button"
    - "Clicking the Back button on privacy returns to the screen the user came from (returnTo), or `'intake'` if none"
    - "Privacy text mentions: no backend of our own, no analytics, no third-party scripts, no microphone access ever (Art. 179bis), no cookies, key in localStorage, what is sent on each Generate, the Anthropic privacy URL"
    - "REQUIREMENTS.md TRUST-05 line says `Anthropic API key`, NOT `free Gemini API key`"
    - "REQUIREMENTS.md contains zero remaining references to Gemini"
    - "index.html contains zero references to Gemini in user-visible copy"
    - "README.md documents the first-visit onboarding screen and the Anthropic Console link"
    - "verify-no-mic.ps1 still exits 0"
    - "CSP meta tag is unchanged"
  artifacts:
    - path: "index.html"
      provides: "`ANTHROPIC_CONSOLE_URL` const, `ONBOARDING_STORAGE_KEY` const, `loadOnboardingSeen` / `saveOnboardingSeen` helpers, `appState.returnTo` field, `<section id=\"onboarding\">` and `<section id=\"privacy\">` DOM, two new render() switch cases, Privacy link in the page `<footer>` and inside `.cheat-sheet-controls`, click handlers for onboarding Continue and Privacy Back, DOMContentLoaded routing extension to choose `'onboarding'` when the flag is absent"
      contains: "ANTHROPIC_CONSOLE_URL"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRUST-05 wording amended to drop `free Gemini API key` and say `Anthropic API key`"
      contains: "Anthropic API key"
    - path: "README.md"
      provides: "New section briefly documenting the first-visit onboarding screen and the Anthropic Console URL"
      contains: "console.anthropic.com"
  key_links:
    - from: "DOMContentLoaded boot"
      to: "render() with initial screen = 'onboarding' when ONBOARDING_STORAGE_KEY is absent"
      via: "New conditional inserted after the existing key-load block, before the existing `appState.screen` initial assignment"
      pattern: "ONBOARDING_STORAGE_KEY"
    - from: "Onboarding Continue button click"
      to: "localStorage write + setState({ screen: apiKey ? 'intake' : 'key-entry' })"
      via: "Single handler `onOnboardingContinue`, mirrors `saveConsent` shape from Phase 2"
      pattern: "onOnboardingContinue"
    - from: "Privacy link click (footer OR cheat-sheet controls)"
      to: "setState({ screen: 'privacy' }) with appState.returnTo populated"
      via: "Handler `onPrivacyClick` reads appState.screen, writes it to appState.returnTo, then transitions"
      pattern: "onPrivacyClick"
    - from: "Privacy Back button click"
      to: "setState({ screen: appState.returnTo || 'intake' }) and clear returnTo"
      via: "Handler `onPrivacyBack`"
      pattern: "onPrivacyBack"
---

<objective>
Add the first-visit onboarding flow and the dedicated privacy page, and amend REQUIREMENTS.md to reflect the Anthropic-only architecture locked in Phase 0 (TRUST-05 wording housekeeping). Two new screens (`'onboarding'`, `'privacy'`) plug into the existing setState + render machinery. The privacy page is reachable from every non-result screen via a footer link, and from the result screen via a Privacy link inside `.cheat-sheet-controls` (the page footer is hidden in print). Onboarding is gated by a new `migrationsamt.onboardingSeen` boolean in localStorage.

Purpose: Satisfy TRUST-03 (the privacy page in plain English) and TRUST-05 (first-visit onboarding explaining BYO key + link to Anthropic Console). Close the last open Phase 3 requirements.

Output: Modified `index.html` (DOM + JS additions only, no CSS changes beyond what the print stylesheet already hides), amended `.planning/REQUIREMENTS.md`, and updated `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-CONTEXT.md
@.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md
@.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-01-PLAN.md
@.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-02-PLAN.md
@index.html
@README.md
@CLAUDE.md

<interfaces>
<!-- appState (Phase 2) currently includes: screen, apiKey, lastIntake, cheatSheet, errorClass, errorDetail, consentGiven.
     This plan adds: returnTo (string|null). The screen union widens to include 'onboarding' and 'privacy'. -->
<!-- Existing storage keys (Phase 1 / Phase 2):
       migrationsamt.apiKey
       migrationsamt.consentGiven
     New key:
       migrationsamt.onboardingSeen -->
<!-- DOMContentLoaded handler (Phase 1) at the bottom of <script> currently:
       1. Loads consent flag.
       2. Loads API key.
       3. Sets appState.screen to 'intake' if key, else leaves 'key-entry'.
       4. Calls render().
     This plan inserts a step 2.5: load onboardingSeen; if missing/false set appState.screen = 'onboarding' (overriding step 3). -->
<!-- Plan 03-02's @media print already hides #onboarding and #privacy in print, so this plan does NOT need new print rules. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend appState (add `returnTo`), add ANTHROPIC_CONSOLE_URL + ONBOARDING_STORAGE_KEY constants, add loadOnboardingSeen / saveOnboardingSeen helpers, extend boot path to route to `'onboarding'` on first visit</name>
  <files>index.html</files>
  <read_first>
    - index.html lines ~750 to ~770 (appState declaration, CONSENT_STORAGE_KEY constant)
    - index.html lines ~1585 to ~1600 (DOMContentLoaded handler, current boot routing)
    - .planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md Section 6 (trigger logic, storage key naming, Anthropic Console URL recommendation), Section 11 (TRUST-05)
  </read_first>
  <action>
    1. Update the screen-union JSDoc / comment above `appState` (~line 740-750) to include the two new screens:

       `// 'key-entry' | 'intake' | 'generating' | 'result' | 'error' | 'onboarding' | 'privacy'`

    2. Add a new field to the `appState` initial object: `returnTo: null,` immediately after the existing fields, with an inline comment: `// 'privacy' navigation: holds the screen the user came from; consumed by Privacy Back. Default null.`.

    3. Add two new module-level constants near `CONSENT_STORAGE_KEY` (~line 768). Each gets a banner comment in the existing 70-dash style:

       ```
       const ONBOARDING_STORAGE_KEY = 'migrationsamt.onboardingSeen';
       const ANTHROPIC_CONSOLE_URL = 'https://console.anthropic.com';
       ```

       Comment for ANTHROPIC_CONSOLE_URL: `Base Anthropic Console URL. Linked from the onboarding screen and the privacy page. Base URL (not /settings/keys) so unsigned-up users see the sign-up flow first (per 03-RESEARCH.md Section 6).`

    4. Add two helper functions next to the existing `loadConsent` / `saveConsent` pair (~line 770 area):

       ```
       function loadOnboardingSeen() {
         try {
           return localStorage.getItem(ONBOARDING_STORAGE_KEY) === 'true';
         } catch (_e) {
           // localStorage blocked: pretend the flag isn't there so the user sees onboarding.
           return false;
         }
       }

       function saveOnboardingSeen() {
         try {
           localStorage.setItem(ONBOARDING_STORAGE_KEY, 'true');
         } catch (_e) {
           // Best effort; if storage is blocked the user sees onboarding again next visit.
         }
       }
       ```

       Each function gets a one-paragraph comment in plain language.

    5. Extend the DOMContentLoaded handler. Locate the current sequence (load consent, load key, set initial screen, render). Insert a new conditional AFTER the key-load and BEFORE the existing screen assignment:

       ```
       const onboardingSeen = loadOnboardingSeen();
       if (!onboardingSeen) {
         appState.screen = 'onboarding';
       } else if (appState.apiKey) {
         appState.screen = 'intake';
       } else {
         appState.screen = 'key-entry';
       }
       ```

       Replace the existing key-vs-no-key assignment with this combined block; the new block subsumes it. Add an inline comment: `Onboarding takes priority on first visit; subsequent visits use the Phase 1 routing.`

    Hard constraints:
    - No `.innerHTML =` introduced.
    - No em or en dashes.
    - Both helpers are wrapped in try/catch so localStorage exceptions never crash the boot path (matches existing `loadConsent` defensive pattern).
    - The `appState.returnTo` field is `null` by default and only ever holds a string when Privacy is navigated to.
    - CSP not touched.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$src = Get-Content -Raw -Path index.html; $checks = @{ screenUnionUpdated = ($src -match \"'onboarding'[^|]*\\|[^|]*'privacy'\") -or ($src -match \"'privacy'[^|]*\\|[^|]*'onboarding'\") -or (($src -match \"'onboarding'\") -and ($src -match \"'privacy'\")); appStateReturnTo = ($src -match 'returnTo:\\s*null'); onboardingKeyConst = ($src -match \"const\\s+ONBOARDING_STORAGE_KEY\\s*=\\s*'migrationsamt\\.onboardingSeen'\"); anthropicConsoleConst = ($src -match \"const\\s+ANTHROPIC_CONSOLE_URL\\s*=\\s*'https://console\\.anthropic\\.com'\"); loadOnboardingFn = ($src -match 'function\\s+loadOnboardingSeen\\s*\\('); saveOnboardingFn = ($src -match 'function\\s+saveOnboardingSeen\\s*\\('); loadUsesGetItem = ($src -match 'localStorage\\.getItem\\(\\s*ONBOARDING_STORAGE_KEY'); saveUsesSetItem = ($src -match 'localStorage\\.setItem\\(\\s*ONBOARDING_STORAGE_KEY'); bootRoutesOnboarding = ($src -match \"!\\s*onboardingSeen[\\s\\S]{0,200}screen\\s*=\\s*'onboarding'\") -or ($src -match \"appState\\.screen\\s*=\\s*'onboarding'\"); noEmDash = -not $src.Contains([char]0x2014); noEnDash = -not $src.Contains([char]0x2013); noGetUserMedia = -not ($src -match 'getUserMedia'); cspIntact = ($src -match 'connect-src[^\"]*api\\.anthropic\\.com') }; $fail = 0; foreach ($k in $checks.Keys) { Write-Output \"$k=$($checks[$k])\"; if (-not $checks[$k]) { $fail++ } }; & .\\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $fail++ }; exit $fail"</automated>
    <acceptance_criteria>
      - All 13 checks pass.
      - `appState.returnTo` initialised to `null`.
      - ONBOARDING_STORAGE_KEY = `'migrationsamt.onboardingSeen'`.
      - ANTHROPIC_CONSOLE_URL = `'https://console.anthropic.com'`.
      - Boot path routes to `'onboarding'` when the flag is missing.
      - `verify-no-mic.ps1` exits 0.
    </acceptance_criteria>
  </verify>
  <done>
    State machine and storage layer are extended. The boot routing now considers onboarding-seen ahead of key presence. Plan 03-03 Task 2 wires the actual DOM and copy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `<section id="onboarding">` and `<section id="privacy">` DOM with verbatim copy from Section 6 + Section 7; extend render() with two new cases; extend hideAllSections to sweep them</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md Section 6 (full onboarding body copy verbatim, Continue handler shape), Section 7 (full privacy body copy verbatim, returnTo navigation)
    - index.html `<main>` children (post-Phase-2 + post-Plan-03-01), to find a clean spot to add two new sections AHEAD of `#result`
    - index.html `hideAllSections` helper (~line 831) and the render() switch (~line 985)
  </read_first>
  <action>
    Add two new `<section>` elements inside `<main>`, each `hidden` by default, placed AHEAD of `#result` so the DOM order is: `#key-entry`, `#intake`, `#generating`, `#onboarding`, `#privacy`, `#result`, `#errorPanel` (or whatever the existing order is; insert `#onboarding` and `#privacy` ahead of `#result`).

    **1. `<section id="onboarding" hidden>`** - body copy verbatim from 03-RESEARCH.md Section 6, structured as HTML semantic elements (no `.innerHTML` injection; this is static HTML hand-written in the template):

    - `<h2>Welcome</h2>`
    - `<p>This is a preparation aid for calling the Migrationsamt of canton Zurich. You describe your situation in your own language, and the tool builds a German cheat sheet you keep in front of you while you make the call yourself.</p>` (Note: use `Zurich` without the umlaut here to keep the executor-friendly copy ASCII-clean; the Migrationsamt's German name uses `Zürich` only inside the DE constants. Either form is acceptable as long as it is consistent within this onboarding paragraph.)
    - `<h3>How it works</h3>`
    - `<p>You bring your own Anthropic API key. The key is saved only in your browser (in localStorage), never sent to any server we run. There is no backend, no account, no log file. When you press Generate, your intake answers are sent in a single call to Anthropic, and Anthropic's response (the cheat sheet) comes straight back to your browser.</p>`
    - `<h3>You will need an Anthropic API key</h3>`
    - `<p>If you do not have one yet, sign up at the Anthropic Console, then go to Settings, API Keys, Create Key. Copy the key (it starts with sk-ant-...) and paste it into the next screen.</p>`
    - `<p><a id="onboardingAnthropicLink" href="https://console.anthropic.com" target="_blank" rel="noopener noreferrer">Get an Anthropic API key</a></p>` (the `href` MUST match `ANTHROPIC_CONSOLE_URL`; this is the link target referenced by the verify step. The executor MAY ALSO set `onboardingAnthropicLink.href = ANTHROPIC_CONSOLE_URL` once at boot time so the constant stays the single source of truth.)
    - `<h3>What this tool will not do</h3>`
    - `<ul>` containing three `<li>` items: `It is not legal advice. It does not tell you what permit you should apply for, or what to say to influence an outcome.`, `It does not contact the Migrationsamt for you. You make the call yourself.`, `It never accesses your microphone. There is no recording of any kind, ever.`
    - `<p>Read more about how your data is handled on the Privacy page (link in the page footer).</p>`
    - `<button type="button" id="onboardingContinueButton">Got it, continue</button>`

    **2. `<section id="privacy" hidden>`** - body copy verbatim from 03-RESEARCH.md Section 7:

    - `<h2>Privacy</h2>`
    - `<p>This tool is designed so that as little of your information as possible leaves your browser.</p>`
    - `<h3>What stays in your browser, only</h3>`
    - `<ul>` with four `<li>` entries verbatim from Section 7 (Anthropic API key in localStorage, the intake answers, the generated cheat sheet, the two boolean flags).
    - `<h3>What is sent over the network</h3>`
    - `<p>When you press Generate cheat sheet, your intake answers plus the system prompt are sent in one HTTPS request to Anthropic's API (https://api.anthropic.com/v1/messages). The response (the cheat sheet) comes straight back to your browser. Anthropic's own data-handling policies apply to that request: see <a href="https://www.anthropic.com/legal/privacy" target="_blank" rel="noopener noreferrer">https://www.anthropic.com/legal/privacy</a></p>`
    - `<p>That is the only outbound request this tool ever makes.</p>`
    - `<h3>What this tool never does</h3>`
    - `<ul>` with four `<li>` entries: no backend of our own; no analytics, no tracking pixels, no third-party scripts (CSP blocks them); no microphone access ever (Article 179bis Swiss Criminal Code, permanent design constraint, even future versions); no cookies are set by this tool.
    - `<h3>Your right to walk away</h3>`
    - `<p>You can clear your saved key in one click (Clear key on any screen). You can close the tab and nothing about your session persists except the saved key (if you saved one) and the two boolean flags. Clearing your browser site data removes everything.</p>`
    - `<button type="button" id="privacyBackButton">Back</button>`

    **3. Extend `hideAllSections`** to also hide `#onboarding` and `#privacy`. Inspect the existing helper; if it iterates an array of ids or selectors, append `'onboarding'` and `'privacy'` to that list. If it sets `hidden = true` on each section individually, add two lines for the new ids.

    **4. Extend `render()`** to add two new switch cases:

    ```
    case 'onboarding': {
      hideAllSections();
      const el = document.getElementById('onboarding');
      el.hidden = false;
      // Keep the link href in sync with ANTHROPIC_CONSOLE_URL in case it ever changes.
      const link = document.getElementById('onboardingAnthropicLink');
      if (link) link.href = ANTHROPIC_CONSOLE_URL;
      break;
    }
    case 'privacy': {
      hideAllSections();
      const el = document.getElementById('privacy');
      el.hidden = false;
      break;
    }
    ```

    Both cases follow the same pattern as `'intake'`, `'generating'`, `'result'`, `'error'`. No state changes inside render; render only paints.

    Hard constraints:
    - All body copy is HARD-CODED in the HTML template (not injected via JS). No `.innerHTML =`.
    - The Anthropic Console URL appears verbatim as `https://console.anthropic.com` in the `<a>` href AND is also reachable via `ANTHROPIC_CONSOLE_URL`.
    - No em or en dashes in any of the copy. Where Section 6 / Section 7 use a parenthetical instead of a dash, copy it parenthetical-form.
    - No reference to `Gemini` anywhere in the new copy or the surrounding code.
    - The cheat-sheet anti-XSS posture is preserved: the new sections contain only hand-written HTML, no template substitution from untrusted strings.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$src = Get-Content -Raw -Path index.html; $checks = @{ onboardingSection = ($src -match '<section id=\"onboarding\"[^>]*hidden'); privacySection = ($src -match '<section id=\"privacy\"[^>]*hidden'); welcomeHeading = ($src -match '<h2>Welcome</h2>'); privacyHeading = ($src -match '<h2>Privacy</h2>'); continueButton = ($src -match 'id=\"onboardingContinueButton\"'); backButton = ($src -match 'id=\"privacyBackButton\"'); anthropicLinkInOnboarding = ($src -match 'id=\"onboardingAnthropicLink\"'); anthropicHref = ($src -match 'href=\"https://console\\.anthropic\\.com\"'); anthropicPrivacyLink = ($src -match 'https://www\\.anthropic\\.com/legal/privacy'); covers179bis = ($src -match 'Article 179bis'); coversNoCookies = ($src -match 'No cookies'); coversNoAnalytics = ($src -match 'no analytics' -or $src -match 'No analytics'); coversNoMicEver = ($src -match 'never accesses your microphone') -or ($src -match 'No microphone access, ever'); renderOnboardingCase = ($src -match \"case\\s*'onboarding'\"); renderPrivacyCase = ($src -match \"case\\s*'privacy'\"); noInnerHtmlAssign = -not ($src -match '\\.innerHTML\\s*='); noGemini = -not ($src -match 'Gemini'); noEmDash = -not $src.Contains([char]0x2014); noEnDash = -not $src.Contains([char]0x2013); noGetUserMedia = -not ($src -match 'getUserMedia'); cspIntact = ($src -match 'connect-src[^\"]*api\\.anthropic\\.com') }; $fail = 0; foreach ($k in $checks.Keys) { Write-Output \"$k=$($checks[$k])\"; if (-not $checks[$k]) { $fail++ } }; & .\\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $fail++ }; exit $fail"</automated>
    <acceptance_criteria>
      - All 22 checks pass.
      - The `#onboarding` and `#privacy` sections exist as `<section>` elements with `hidden` by default.
      - The Continue and Back buttons have the locked ids.
      - The Anthropic Console URL points to the base domain (NOT `/settings/keys`).
      - The privacy copy covers no-backend, no-analytics, no-third-party-scripts, no-microphone-ever (Article 179bis), no cookies.
      - `Gemini` appears zero times anywhere in `index.html`.
      - No `.innerHTML =` introduced.
    </acceptance_criteria>
  </verify>
  <done>
    Onboarding and Privacy DOM are present, render() handles both, and hideAllSections cleans them up on every transition.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire onboarding Continue, Privacy Back, and Privacy link click handlers; render the Privacy link in the page footer and inside `.cheat-sheet-controls`</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md Section 6 (onOnboardingContinue), Section 7 (onPrivacyClick + onPrivacyBack)
    - index.html (post-Task-2) event-handlers section (where existing button wires live)
    - index.html `<footer>` element (the page-level footer) and the `.cheat-sheet-controls` div from Plan 03-01
  </read_first>
  <action>
    1. Add three new handlers in the event-handlers section, in the order they will be wired:

       ```
       function onOnboardingContinue() {
         saveOnboardingSeen();
         setState({ screen: appState.apiKey ? 'intake' : 'key-entry' });
       }

       function onPrivacyClick() {
         // Remember where the user came from so Back can return there.
         appState.returnTo = appState.screen;
         setState({ screen: 'privacy' });
       }

       function onPrivacyBack() {
         const target = appState.returnTo || 'intake';
         appState.returnTo = null;
         setState({ screen: target });
       }
       ```

       Inline comments explain each function in plain language.

    2. Wire the Continue and Back buttons in the DOMContentLoaded handler (or wherever the other button wires live):

       ```
       document.getElementById('onboardingContinueButton')
         .addEventListener('click', onOnboardingContinue);
       document.getElementById('privacyBackButton')
         .addEventListener('click', onPrivacyBack);
       ```

    3. Add a Privacy link in the existing page `<footer>` element. The link must be visible on every non-result screen. The simplest pattern: edit the existing `<footer>` markup to include `<a href="#" id="footerPrivacyLink">Privacy</a>` (use a JS-prevented `<a>` with `href="#"`, attach the handler in JS to call `event.preventDefault()` + `onPrivacyClick()`). Reason for `<a>` over `<button>`: the footer reads as plain copy and a button looks heavy; the `preventDefault` keeps URL hash clean.

       Wire the handler:

       ```
       const footerLink = document.getElementById('footerPrivacyLink');
       if (footerLink) {
         footerLink.addEventListener('click', function (event) {
           event.preventDefault();
           onPrivacyClick();
         });
       }
       ```

       Note: the print stylesheet (Plan 03-02) already hides the page `<footer>` in print, so this Privacy link does NOT appear on the printed sheet. The print rule also already hides `#onboarding` and `#privacy` content.

    4. Add a Privacy link inside `.cheat-sheet-controls` on the result screen (because the page `<footer>` is hidden in print AND the result screen relies on the controls bar for screen-only navigation). Plan 03-01 introduced `.cheat-sheet-controls` with Print + New + Back buttons; this task ADDS a Privacy link to that bar AFTER the Back button:

       ```
       <a href="#" id="resultPrivacyLink" class="result-privacy-link">Privacy</a>
       ```

       (Add a small inline rule to the existing stylesheet so the link sits inline with the buttons; e.g. `.result-privacy-link { margin-left: auto; font-size: 0.95rem; }`.)

       Wire it the same way:

       ```
       const resultLink = document.getElementById('resultPrivacyLink');
       if (resultLink) {
         resultLink.addEventListener('click', function (event) {
           event.preventDefault();
           onPrivacyClick();
         });
       }
       ```

       The `.cheat-sheet-controls` element is already `display: none !important` in `@media print` (Plan 03-02), so this Privacy link is also automatically hidden from print.

    Hard constraints:
    - No `.innerHTML =` introduced.
    - The Privacy link is a real anchor with `href="#"` + `preventDefault`. Do not use plain `<span>` with a click handler (accessibility regression).
    - `appState.returnTo` is the ONLY state field used for back navigation. Do not rely on browser history.
    - `onOnboardingContinue` does NOT touch `appState.consentGiven` (consent is a separate Phase 2 flag).
    - No em or en dashes in code, copy, or comments.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$src = Get-Content -Raw -Path index.html; $checks = @{ onOnboardingFn = ($src -match 'function\\s+onOnboardingContinue\\s*\\('); onPrivacyClickFn = ($src -match 'function\\s+onPrivacyClick\\s*\\('); onPrivacyBackFn = ($src -match 'function\\s+onPrivacyBack\\s*\\('); continueButtonWired = ($src -match \"getElementById\\(\\s*'onboardingContinueButton'\\s*\\)[\\s\\S]{0,300}onOnboardingContinue\"); backButtonWired = ($src -match \"getElementById\\(\\s*'privacyBackButton'\\s*\\)[\\s\\S]{0,300}onPrivacyBack\"); footerLinkExists = ($src -match 'id=\"footerPrivacyLink\"'); resultLinkExists = ($src -match 'id=\"resultPrivacyLink\"'); footerLinkPreventDefault = ($src -match \"footerPrivacyLink[\\s\\S]{0,400}preventDefault\"); resultLinkPreventDefault = ($src -match \"resultPrivacyLink[\\s\\S]{0,400}preventDefault\"); returnToWriteOnClick = ($src -match 'appState\\.returnTo\\s*=\\s*appState\\.screen'); returnToClearOnBack = ($src -match 'appState\\.returnTo\\s*=\\s*null'); savesOnboarding = ($src -match 'saveOnboardingSeen\\s*\\(\\s*\\)'); noInnerHtmlAssign = -not ($src -match '\\.innerHTML\\s*='); noEmDash = -not $src.Contains([char]0x2014); noEnDash = -not $src.Contains([char]0x2013); noGetUserMedia = -not ($src -match 'getUserMedia') }; $fail = 0; foreach ($k in $checks.Keys) { Write-Output \"$k=$($checks[$k])\"; if (-not $checks[$k]) { $fail++ } }; & .\\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $fail++ }; exit $fail"</automated>
    <acceptance_criteria>
      - All 16 checks pass.
      - Both Privacy links (`#footerPrivacyLink` and `#resultPrivacyLink`) exist, and both call `preventDefault` in their handlers.
      - `appState.returnTo` is written on Privacy entry and cleared on Privacy back.
      - `onOnboardingContinue` calls `saveOnboardingSeen()`.
      - `verify-no-mic.ps1` exits 0.
    </acceptance_criteria>
  </verify>
  <done>
    All three handlers are live. Privacy is reachable from key-entry / intake / onboarding / error via the footer link, and from the result screen via the controls-bar link. Onboarding Continue saves the flag and routes correctly based on key presence.
  </done>
</task>

<task type="auto">
  <name>Task 4: Amend REQUIREMENTS.md TRUST-05 wording and update README.md to document the onboarding flow</name>
  <files>.planning/REQUIREMENTS.md, README.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 49 (current TRUST-05 text: `A first-visit onboarding screen explains how to get a free Gemini API key, with link`)
    - README.md (existing Phase 1 + Phase 2 sections)
    - .planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md Section 11 (housekeeping rationale)
  </read_first>
  <action>
    1. **REQUIREMENTS.md amendment.** Edit line 49 (the TRUST-05 bullet). Replace:

       `- [ ] **TRUST-05**: A first-visit onboarding screen explains how to get a free Gemini API key, with link`

       with:

       `- [ ] **TRUST-05**: A first-visit onboarding screen explains how to get an Anthropic API key, with link`

       Drop the word `free` (Anthropic is paid pay-as-you-go, very cheap, not free).

       Do NOT change any other line in REQUIREMENTS.md. Do NOT mark TRUST-05 as `[x]` here - the plan SUMMARY records the verification and the GSD transition workflow handles status promotion.

       Verify the file no longer contains any reference to `Gemini` anywhere.

    2. **README.md update.** Add a new short section titled `First-visit onboarding` near the existing user-facing sections (preserve all existing content). Content:

       ```
       ## First-visit onboarding

       On the first visit, the app shows a brief onboarding screen explaining the BYO-key model and pointing to the Anthropic Console (https://console.anthropic.com) to create an API key. A Privacy link in the page footer (and in the cheat-sheet controls bar) opens the privacy page on demand. After dismissing onboarding once, the app remembers it via a `migrationsamt.onboardingSeen` localStorage flag; clearing browser site data makes onboarding show again.
       ```

       Add this section AT THE BOTTOM of README.md unless a more natural slot exists (e.g. between an existing `Live URL` and `Known traps` section).

       No em or en dashes, no `Gemini` reference.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$req = Get-Content -Raw -Path .planning/REQUIREMENTS.md; $readme = Get-Content -Raw -Path README.md; $checks = @{ trust05Amended = ($req -match 'TRUST-05[^\\n]*Anthropic API key'); trust05NotGemini = -not ($req -match 'free Gemini API key'); reqNoGeminiAnywhere = -not ($req -match 'Gemini'); readmeOnboardingSection = ($readme -match 'First-visit onboarding'); readmeAnthropicConsole = ($readme -match 'console\\.anthropic\\.com'); readmeStorageKey = ($readme -match 'migrationsamt\\.onboardingSeen'); readmeNoGemini = -not ($readme -match 'Gemini'); reqNoEmDash = -not $req.Contains([char]0x2014); reqNoEnDash = -not $req.Contains([char]0x2013); readmeNoEmDash = -not $readme.Contains([char]0x2014); readmeNoEnDash = -not $readme.Contains([char]0x2013) }; $fail = 0; foreach ($k in $checks.Keys) { Write-Output \"$k=$($checks[$k])\"; if (-not $checks[$k]) { $fail++ } }; & .\\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $fail++ }; exit $fail"</automated>
    <acceptance_criteria>
      - All 11 checks pass.
      - REQUIREMENTS.md TRUST-05 line says `Anthropic API key`, NOT `free Gemini API key`.
      - REQUIREMENTS.md contains zero references to `Gemini`.
      - README.md has a `First-visit onboarding` section that references the Anthropic Console URL and the storage key.
      - No em/en dashes in either file.
      - `verify-no-mic.ps1` exits 0.
    </acceptance_criteria>
  </verify>
  <done>
    TRUST-05 housekeeping is closed. README.md documents the new boot-time onboarding behaviour.
  </done>
</task>

<task type="auto">
  <name>Task 5: End-to-end smoke check (automated) - clear-localStorage + ?mock=1 boot lands on onboarding; Privacy link reachable on every non-result screen; clean grep for Gemini and innerHTML</name>
  <files>index.html</files>
  <read_first>
    - The four prior tasks' outputs (no re-read of the file content required; the verify script greps for the artefacts listed below)
  </read_first>
  <action>
    No code edit. This task runs an integration-level automated verify against the final state of `index.html` to catch regressions introduced by Tasks 1-4 interacting. The verify confirms:

    - Both new sections (`#onboarding`, `#privacy`) exist.
    - The Privacy link is present in the page footer AND in the cheat-sheet controls bar.
    - The Continue button calls saveOnboardingSeen.
    - The boot routing references the onboarding flag before the key flag.
    - hideAllSections sweeps the two new sections.
    - There are zero references to `Gemini` anywhere in `index.html`.
    - There are zero `.innerHTML =` assignments anywhere in `index.html`.
    - The CSP `connect-src` is still `api.anthropic.com` only.
    - The lifetime mic guardrail (`verify-no-mic.ps1`) exits 0.

    If any check fails, the executor goes back to whichever task introduced the regression, fixes it, and re-runs both the task's own verify and this integration verify.

    The executor optionally performs a manual smoke check (NOT gated by automated verify):
    - Open `index.html?mock=1` in Chrome with localStorage cleared. Confirm onboarding renders, the Anthropic link points to `https://console.anthropic.com`, Continue advances to key-entry (no key saved) or intake (key already saved).
    - From intake, click Privacy in the footer. Confirm privacy page renders with all four sections (`What stays in your browser, only`, `What is sent over the network`, `What this tool never does`, `Your right to walk away`). Click Back. Confirm return to intake.
    - Reload. Confirm onboarding does NOT show again.
    - Submit intake with `?mock=1`. Confirm the result screen renders with the disclaimer and all eight blocks; the Privacy link is present in the controls bar.

    This manual walkthrough is recorded in the 03-03-SUMMARY.md; it is NOT a separate human-verify checkpoint task (Phase 3's only formal checkpoint is the print walkthrough in Plan 03-02).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$src = Get-Content -Raw -Path index.html; $checks = @{ onboardingSection = ($src -match '<section id=\"onboarding\"'); privacySection = ($src -match '<section id=\"privacy\"'); footerPrivacyLink = ($src -match 'id=\"footerPrivacyLink\"'); resultPrivacyLink = ($src -match 'id=\"resultPrivacyLink\"'); continueSaves = ($src -match 'onOnboardingContinue[\\s\\S]{0,300}saveOnboardingSeen'); bootChecksFlag = ($src -match 'loadOnboardingSeen\\s*\\(\\s*\\)') -and ($src -match \"appState\\.screen\\s*=\\s*'onboarding'\"); hideSweepsOnboarding = ($src -match 'hideAllSections[\\s\\S]{0,2000}onboarding') -or ($src -match \"document\\.getElementById\\(\\s*'onboarding'\\s*\\)\\.hidden\\s*=\\s*true\"); hideSweepsPrivacy = ($src -match 'hideAllSections[\\s\\S]{0,2000}privacy') -or ($src -match \"document\\.getElementById\\(\\s*'privacy'\\s*\\)\\.hidden\\s*=\\s*true\"); noGemini = -not ($src -match 'Gemini'); noInnerHtml = -not ($src -match '\\.innerHTML\\s*='); cspIntact = ($src -match 'connect-src[^\"]*api\\.anthropic\\.com'); cspNoExtraConnect = -not ($src -match 'connect-src[^\"]*(googleapis|openai|gstatic|cdn\\.)'); noEmDash = -not $src.Contains([char]0x2014); noEnDash = -not $src.Contains([char]0x2013); noGetUserMedia = -not ($src -match 'getUserMedia') }; $fail = 0; foreach ($k in $checks.Keys) { Write-Output \"$k=$($checks[$k])\"; if (-not $checks[$k]) { $fail++ } }; & .\\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $fail++ }; exit $fail"</automated>
    <acceptance_criteria>
      - All 15 checks pass.
      - Both onboarding/privacy sections are wired and reachable.
      - hideAllSections sweeps both new sections (either by name in an id-array OR by explicit `hidden = true` set).
      - Zero `Gemini` references anywhere in `index.html`.
      - Zero `.innerHTML =` assignments anywhere in `index.html`.
      - CSP `connect-src` references `api.anthropic.com` and nothing else third-party.
      - `verify-no-mic.ps1` exits 0.
    </acceptance_criteria>
  </verify>
  <done>
    Plan 03-03 is integration-clean. Tasks 1-4 have not regressed each other. Phase 3 is ready to transition.
  </done>
</task>

</tasks>

<verification>
- Tasks 1, 2, 3, 4, 5 each exit 0 on their automated verify.
- `verify-no-mic.ps1` exits 0.
- REQUIREMENTS.md no longer references `Gemini` and TRUST-05 is amended.
- README.md documents the first-visit onboarding behaviour and the Anthropic Console URL.
- `index.html` contains zero `.innerHTML =` assignments and zero `Gemini` references.
- The CSP meta tag is unchanged.
- Manual smoke walkthrough by the executor confirms: clear localStorage -> onboarding shows -> Continue -> key-entry/intake -> Privacy link works from intake -> Back returns -> reload does not re-show onboarding -> result screen still includes the Privacy link in the controls bar. Recorded in the SUMMARY.
</verification>

<success_criteria>
- TRUST-03: dedicated privacy page reachable from a Privacy link on every non-result screen AND on the result screen via the controls bar; covers no-backend, no-analytics, no-third-party-scripts, no-microphone-ever (Article 179bis), no cookies, localStorage scope, what is sent on each Generate, link to Anthropic's privacy policy.
- TRUST-05: first-visit onboarding screen explains the BYO Anthropic API key model and links to `https://console.anthropic.com`. The REQUIREMENTS.md wording is amended.
- TRUST-04 carry-forward: `verify-no-mic.ps1` exits 0; no `getUserMedia` introduced.
- L-05 carry-forward: every dynamic write via `textContent`; the new sections use hand-written static HTML (no template substitution from untrusted strings); zero `.innerHTML =` assignments in `index.html`.
- L-07 carry-forward: CSP unchanged, no CDN, no new font, no third-party origin in connect-src.
</success_criteria>

<output>
After Task 5 verifies, create `.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- The two new screens (`'onboarding'`, `'privacy'`) and the updated screen union shape.
- The new constants (`ANTHROPIC_CONSOLE_URL`, `ONBOARDING_STORAGE_KEY`) and helpers (`loadOnboardingSeen`, `saveOnboardingSeen`, `onOnboardingContinue`, `onPrivacyClick`, `onPrivacyBack`).
- The `appState.returnTo` field added for Privacy back-navigation.
- The exact wording shipped in the onboarding screen and the privacy page (Section 6 + Section 7 verbatim; flag any deviations).
- Where the Privacy link is rendered (footer + cheat-sheet-controls) and why both placements exist (page footer is hidden in print and on the result screen).
- TRUST-05 housekeeping outcome: REQUIREMENTS.md line 49 amended; zero `Gemini` references remaining in REQUIREMENTS.md, index.html, README.md.
- README.md addition (the `First-visit onboarding` section).
- Confirmation that `verify-no-mic.ps1` exits 0 and the CSP meta tag is unchanged.
- Any deviations from this plan.
- Items deferred to Phase 4 (multilingual onboarding/privacy copy) and Phase 5 (real-user pilot of the onboarding flow + hard PRINT-02 pass on a real iPhone).
</output>
