---
phase: 03-cheat-sheet-rendering-print-mobile-onboarding-privacy
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - index.html
autonomous: false
requirements:
  - PRINT-01
  - PRINT-02
  - PRINT-03
tags: [print, mobile, media-print, media-mobile-480, page-break, block-g, ios-safari, phase-3]
decisions:
  - "Single `<style>` block inside `index.html` is appended with two new rules sections: `@page { size: A4; margin: 1.5cm }` and `@media print { ... }`, plus `@media (max-width: 480px) { ... }`. Per 03-RESEARCH.md Sections 4 and 5."
  - "Authored sequentially after Plan 03-01 (Wave 2) even though logically independent of Plan 03-03 because both Plan 03-02 and Plan 03-03 modify `index.html`. Same-wave plans must have zero `files_modified` overlap; we serialize 03-02 -> 03-03 across waves for safety."
  - "Mobile breakpoint locked at `@media (max-width: 480px)` per Section 5 (covers iPhone 15 portrait 393px, iPhone SE 375px, the standard `<= 480px` phone cutoff). At this breakpoint, `.bilingual-row` collapses to single column, and every form input gets `font-size: 16px` defensively against iOS Safari's auto-zoom."
  - "Print scope: hide every non-cheat-sheet UI by selector (`.no-print`, `#intake`, `#key-entry`, `#generating`, `#errorPanel`, `#onboarding`, `#privacy`, `#rawJsonDebug`, `.cheat-sheet-controls`, the key header strip, the page footer). The cheat-sheet article and the disclaimer print. Block G is revealed via `.cheat-sheet-print-only { display: block !important; }` inside `@media print`."
  - "Page-break behaviour: `break-inside: avoid` + legacy `page-break-inside: avoid` on every `.cheat-sheet-block`. No `page-break-after` rules - per Section 4, let the browser paginate naturally to avoid weird half-empty pages."
  - "`print-color-adjust: exact` + `-webkit-print-color-adjust: exact` on `.cheat-sheet-block` and `.cheat-sheet-disclaimer` so the block borders and disclaimer outline print in Chrome desktop. Section 4 notes the user must keep Chrome's `Background graphics` checkbox ticked; documented in the print-preview checklist."
  - "Block H escalation links print their `href` next to the link text via `#blockH a[href]::after { content: ' (' attr(href) ')'; ... }` so the URLs are usable on paper. Section 4."
  - "Disclaimer behaviour switches between sticky-on-screen (Plan 03-01) and static-at-top-of-page-1 inside `@media print` so it does not visually overlap content on print."
  - "PRINT-02 acceptance is two-stage (Section 5): Phase 3 SOFT PASS via Chrome devtools 'iPhone 15 Pro' emulation in the human-verify checkpoint (Task 3 here); HARD PASS on a real iPhone is deferred to Phase 5 pilot. STATE.md keeps PRINT-02 listed as 'Phase 3 soft pass, Phase 5 hard pass on real iPhone'."
  - "Manual print-preview checklist (10 steps) from 03-RESEARCH.md Section 4 is embedded in the human-verify task verbatim. Francisco runs through it during the checkpoint and reports pass / specific failures."
  - "No new fonts, no @import, no `font-src` CSP widening. System fonts only."
  - "If Plan 03-03 has shipped before this plan executes (alternate sequencing), the `@media print` hide list still works because new screen ids (`#onboarding`, `#privacy`) are listed in the selector. If 03-03 has NOT yet shipped, those ids select nothing and the rule is a no-op - safe either way."

must_haves:
  truths:
    - "Hitting Ctrl+P or clicking the Print button on the result screen produces a one-to-two-page A4 print preview"
    - "No `.cheat-sheet-block` is split across a page break in the print preview against MOCK_CHEAT_SHEET"
    - "The Print button, New cheat sheet button, Back to intake button, raw-JSON details, and the key-header strip are NOT visible in the print preview"
    - "Block G is VISIBLE in the print preview (four labelled blank lines)"
    - "Block A, B, C, D, E, F, H are visible in the print preview"
    - "The TRUST-01 disclaimer renders at the top of page 1 in the print preview, with a clear black border"
    - "On a viewport <= 480px (iPhone 15 Pro / SE emulation), each `.bilingual-row` collapses to a single column"
    - "On a viewport <= 480px, every form input (`input`, `select`, `textarea`) has `font-size: 16px` (defensive against iOS auto-zoom)"
    - "Block H escalation `<a>` elements print their `href` next to their text via the `::after` rule"
    - "verify-no-mic.ps1 still exits 0"
    - "The CSP meta tag is unchanged (same `connect-src`, no new `font-src`, no new `style-src` widening)"
    - "Francisco runs the 10-step manual print-preview checklist and the Chrome devtools iPhone 15 Pro walkthrough and signs off"
  artifacts:
    - path: "index.html"
      provides: "Appended print + mobile CSS rules inside the existing inline `<style>` block: `@page { size: A4; margin: 1.5cm }`, a full `@media print` block hiding non-cheat-sheet UI and forcing Block G visible, and a `@media (max-width: 480px)` block collapsing `.bilingual-row` to single column and pinning form-input font-size to 16px"
      contains: "@media print"
  key_links:
    - from: "window.print() (triggered by #printCheatSheetBtn or Ctrl+P)"
      to: "Browser print engine reading the new `@media print` rules"
      via: "Native browser behaviour; no JS wiring beyond Plan 03-01's click handler"
      pattern: "@media\\s+print"
    - from: "Viewport width <= 480px"
      to: "`.bilingual-row` collapses to single column"
      via: "`@media (max-width: 480px) { .bilingual-row { grid-template-columns: 1fr; ... } }`"
      pattern: "max-width:\\s*480px"
    - from: "`.cheat-sheet-print-only` (Plan 03-01 sets `display: none` on screen)"
      to: "Block G visible in print"
      via: "`@media print { .cheat-sheet-print-only { display: block !important; } }`"
      pattern: "cheat-sheet-print-only"
---

<objective>
Layer the print stylesheet and mobile breakpoint on top of the eight-block cheat sheet shipped by Plan 03-01. This plan delivers PRINT-01 (A4 print layout with no mid-block page breaks), PRINT-02 (single-column readable layout on phone, soft-pass in Phase 3 via Chrome devtools, hard-pass deferred to Phase 5), and PRINT-03 (all non-cheat-sheet UI hidden when printing). Closes with a human-verify checkpoint where Francisco runs the manual print-preview checklist and the iPhone 15 Pro devtools walkthrough.

Purpose: Make the printable cheat sheet actually printable AND readable on a phone. No JS changes, no new dependencies, no CSP changes. All work is appended CSS inside the existing inline `<style>` block.

Output: Modified `index.html` (CSS appended only) plus a recorded human-verify outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-CONTEXT.md
@.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md
@.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-01-PLAN.md
@index.html
@CLAUDE.md

<interfaces>
<!-- Plan 03-01 shipped these classes / ids in index.html:
       .cheat-sheet-disclaimer     - the TRUST-01 strip
       #cheatSheet                 - the eight-block article wrapper
       .cheat-sheet-block          - each of the eight block sections
       .cheat-sheet-print-only     - on #blockG, with `display: none` rule on screen
       .bilingual-row, .de, .en    - two-column DE/EN grid container
       .cheat-sheet-controls       - bar holding Print, New, Back buttons + Ctrl+P hint
       #printCheatSheetBtn         - in-page Print button (Plan 03-01 wired window.print())
       #rawJsonDebug               - dev-only raw JSON details (hidden by default)
-->
<!-- Non-cheat-sheet sections in index.html that must be hidden in print:
       #key-entry, #intake, #generating, #errorPanel
       (Plan 03-03 will add #onboarding and #privacy; selectors for both are included now so the rule is forward-compatible)
       The page-level header strip (intake-key-header) and the page <footer> must also be hidden in print. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `@page` + `@media print` rules to the inline stylesheet</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md Section 4 (full @media print block, page-break rules, Block G reveal, link-URL ::after) and Section 8 (disclaimer print override)
    - index.html (post-03-01) - locate the end of the existing inline `<style>` block and the structure of the key header strip class name (`intake-key-header`) so the hide selector matches exactly
  </read_first>
  <action>
    Append a new banner-commented section at the END of the existing inline `<style>` block:

    ```
    /* ---------------------------------------------------------------------
       PRINT STYLESHEET (Phase 3 Plan 03-02)
       Goal: produce a clean A4 print of the cheat sheet only; hide every
       non-cheat-sheet UI (PRINT-03); avoid mid-block page breaks (PRINT-01);
       reveal Block G note-taking lines only in print.
       --------------------------------------------------------------------- */
    ```

    Then add `@page` and `@media print` blocks verbatim from 03-RESEARCH.md Section 4, with a few specific edits documented below:

    1. `@page { size: A4; margin: 1.5cm; }` - paper + margins.

    2. Inside `@media print { ... }`:

       a. Hide selector list (use `display: none !important;` because the browser sometimes applies inline `hidden` toggles that need overriding):
          ```
          .no-print,
          header,
          footer,
          .intake-key-header,
          #key-entry,
          #intake,
          #generating,
          #errorPanel,
          #onboarding,
          #privacy,
          #rawJsonDebug,
          .cheat-sheet-controls {
            display: none !important;
          }
          ```
          Including `#onboarding` and `#privacy` ahead of Plan 03-03 is intentional (forward-compatible no-op until those ids exist).

       b. Body normalisation:
          ```
          body {
            margin: 0;
            color: #000;
            font-size: 11pt;
          }
          ```

       c. Cheat-sheet wrapper:
          ```
          #cheatSheet {
            max-width: none;
            margin: 0;
            padding: 0;
          }
          ```

       d. Block G reveal:
          ```
          .cheat-sheet-print-only {
            display: block !important;
          }
          ```

       e. Page-break per block (both modern and legacy properties for browser coverage):
          ```
          .cheat-sheet-block {
            page-break-inside: avoid;
            break-inside: avoid;
            margin-bottom: 0.75rem;
          }
          ```

       f. Disclaimer print override (static at the top of page 1, not sticky):
          ```
          .cheat-sheet-disclaimer {
            position: static;
            background: #fff;
            border: 2px solid #000;
            padding: 0.5rem;
            margin-bottom: 0.75rem;
            font-weight: bold;
          }
          ```

       g. Force colored borders/backgrounds to print on Chrome / Edge / Safari:
          ```
          .cheat-sheet-block,
          .cheat-sheet-disclaimer {
            print-color-adjust: exact;
            -webkit-print-color-adjust: exact;
          }
          ```

       h. Block H escalation links print their URLs:
          ```
          #blockH a[href]::after {
            content: " (" attr(href) ")";
            font-size: 0.85em;
            color: #555;
          }
          ```

    Hard constraints:
    - All rules live INSIDE the single existing inline `<style>` block. Do NOT add a second `<style>` element.
    - No `@import`. No external `url()` to a CDN. No `font-face`. System fonts only.
    - CSP meta tag is not edited.
    - No em or en dashes anywhere in the new CSS comments.
    - Ascii hyphens only.
    - Banner comments use the 70-dash style from Plan 01 / Plan 02.

    Note: Plan 03-01 already shipped `.cheat-sheet-print-only { display: none }` for the screen default. This task adds the print override that flips it to `display: block` inside `@media print`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$src = Get-Content -Raw -Path index.html; $checks = @{ pageA4 = ($src -match '@page\\s*\\{[^}]*size:\\s*A4'); pageMargin = ($src -match '@page\\s*\\{[^}]*margin:\\s*1\\.5cm'); mediaPrint = ($src -match '@media\\s+print\\s*\\{'); hidesKeyHeader = ($src -match '@media\\s+print[\\s\\S]{0,2000}\\.intake-key-header'); hidesIntake = ($src -match '@media\\s+print[\\s\\S]{0,2000}#intake'); hidesKeyEntry = ($src -match '@media\\s+print[\\s\\S]{0,2000}#key-entry'); hidesGenerating = ($src -match '@media\\s+print[\\s\\S]{0,2000}#generating'); hidesErrorPanel = ($src -match '@media\\s+print[\\s\\S]{0,2000}#errorPanel'); hidesOnboarding = ($src -match '@media\\s+print[\\s\\S]{0,2000}#onboarding'); hidesPrivacy = ($src -match '@media\\s+print[\\s\\S]{0,2000}#privacy'); hidesRawJson = ($src -match '@media\\s+print[\\s\\S]{0,2000}#rawJsonDebug'); hidesControls = ($src -match '@media\\s+print[\\s\\S]{0,2000}\\.cheat-sheet-controls'); displayNoneImportant = ($src -match '@media\\s+print[\\s\\S]{0,2000}display:\\s*none\\s*!important'); blockGRevealPrint = ($src -match '@media\\s+print[\\s\\S]{0,2000}\\.cheat-sheet-print-only[\\s\\S]{0,200}display:\\s*block\\s*!important'); pageBreakInsideAvoid = ($src -match 'page-break-inside:\\s*avoid'); breakInsideAvoid = ($src -match 'break-inside:\\s*avoid'); printColorAdjustExact = ($src -match 'print-color-adjust:\\s*exact'); webkitPrintColorAdjust = ($src -match '-webkit-print-color-adjust:\\s*exact'); disclaimerPrintStatic = ($src -match '@media\\s+print[\\s\\S]{0,2000}\\.cheat-sheet-disclaimer[\\s\\S]{0,200}position:\\s*static'); blockHHrefAfter = ($src -match '#blockH\\s+a\\[href\\]::after'); cspIntact = ($src -match 'connect-src[^\"]*api\\.anthropic\\.com'); cspNoExtraConnect = -not ($src -match 'connect-src[^\"]*(googleapis|openai|gstatic|cdn\\.)'); noImport = -not ($src -match '@import'); noFontFace = -not ($src -match '@font-face'); noEmDash = -not $src.Contains([char]0x2014); noEnDash = -not $src.Contains([char]0x2013); noGetUserMedia = -not ($src -match 'getUserMedia') }; $fail = 0; foreach ($k in $checks.Keys) { Write-Output \"$k=$($checks[$k])\"; if (-not $checks[$k]) { $fail++ } }; & .\\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $fail++ }; exit $fail"</automated>
    <acceptance_criteria>
      - All 27 named checks pass.
      - `@page` declares `size: A4` and `margin: 1.5cm`.
      - `@media print` hides every non-cheat-sheet UI selector and reveals `.cheat-sheet-print-only`.
      - Both modern (`break-inside`) and legacy (`page-break-inside`) page-break rules are present.
      - `print-color-adjust: exact` (both standard and `-webkit-` prefix) is set on the block + disclaimer.
      - No `@import`, no `@font-face`, no external `url(...)` font loads.
      - CSP unchanged.
      - `verify-no-mic.ps1` exits 0.
    </acceptance_criteria>
  </verify>
  <done>
    Print stylesheet is complete. PRINT-01 (page-break-inside avoidance) and PRINT-03 (all non-cheat-sheet UI hidden) are now CSS-level satisfied. Block G is revealed only in print.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append `@media (max-width: 480px)` mobile breakpoint</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-RESEARCH.md Section 5 (full mobile rationale, 16px form-input defence, line-length note)
    - index.html (post-Task-1) - locate the end of the inline `<style>` block again
  </read_first>
  <action>
    Append a second `@media` block at the end of the inline `<style>` block, AFTER the `@media print` block from Task 1:

    ```
    /* ---------------------------------------------------------------------
       MOBILE BREAKPOINT (Phase 3 Plan 03-02)
       Goal: keep the cheat sheet readable on iPhone Safari (PRINT-02).
       Soft-pass via Chrome devtools iPhone 15 Pro emulation in this phase;
       hard-pass on a real device deferred to the Phase 5 pilot.
       --------------------------------------------------------------------- */
    @media (max-width: 480px) {
      .bilingual-row {
        grid-template-columns: 1fr;
        gap: 0.25rem;
      }
      body {
        font-size: 16px;
      }
      input,
      select,
      textarea {
        font-size: 16px;
      }
    }
    ```

    Rationale (record as a comment above the block):
    - `.bilingual-row` collapses from two columns to one so DE and EN stack vertically (Section 5).
    - `font-size: 16px` on form inputs prevents iOS Safari's auto-zoom on focus (Section 5; CSS Tricks reference). The body's base size is already approximately 16px via the default browser root, but setting it explicitly here is defensive in case future styling drops it.
    - No other rules: line-length and max-width are already handled by the existing screen styles (body `max-width: 720px`).
    - The cheat-sheet block borders, fonts, and spacing inherit from the screen styles - no overrides needed.

    Hard constraints:
    - Single `@media (max-width: 480px)` block. No intermediate breakpoints (e.g. tablet) for v1.
    - Within the same existing inline `<style>` block; no second `<style>` element.
    - No new dependencies, no @import, no @font-face.
    - No em or en dashes.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$src = Get-Content -Raw -Path index.html; $checks = @{ mediaMobile = ($src -match '@media\\s*\\(max-width:\\s*480px\\)'); bilingualSingleCol = ($src -match '@media\\s*\\(max-width:\\s*480px\\)[\\s\\S]{0,500}\\.bilingual-row[\\s\\S]{0,200}grid-template-columns:\\s*1fr'); bodyFontSize16 = ($src -match '@media\\s*\\(max-width:\\s*480px\\)[\\s\\S]{0,800}body[\\s\\S]{0,200}font-size:\\s*16px'); inputFontSize16 = ($src -match '@media\\s*\\(max-width:\\s*480px\\)[\\s\\S]{0,1000}input[\\s\\S]{0,200}font-size:\\s*16px'); noImport = -not ($src -match '@import'); noFontFace = -not ($src -match '@font-face'); cspIntact = ($src -match 'connect-src[^\"]*api\\.anthropic\\.com'); noEmDash = -not $src.Contains([char]0x2014); noEnDash = -not $src.Contains([char]0x2013); noGetUserMedia = -not ($src -match 'getUserMedia') }; $fail = 0; foreach ($k in $checks.Keys) { Write-Output \"$k=$($checks[$k])\"; if (-not $checks[$k]) { $fail++ } }; & .\\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $fail++ }; exit $fail"</automated>
    <acceptance_criteria>
      - All 10 checks pass.
      - `.bilingual-row` collapses to `grid-template-columns: 1fr` at <=480px.
      - All form inputs (`input`, `select`, `textarea`) have `font-size: 16px` at <=480px.
      - No new fonts, no @import.
      - `verify-no-mic.ps1` exits 0.
    </acceptance_criteria>
  </verify>
  <done>
    Mobile breakpoint is in place. PRINT-02 is satisfied at the CSS level; the human-verify checkpoint (Task 3) confirms the visual.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Francisco runs the manual print-preview checklist and the Chrome devtools iPhone 15 Pro walkthrough</name>
  <what-built>
    Plan 03-02 implementation is complete: `@page` A4 with 1.5cm margins, `@media print` block hiding all non-cheat-sheet UI and revealing Block G, `page-break-inside: avoid` plus `break-inside: avoid` on every block, `print-color-adjust: exact` for the disclaimer and block borders, `#blockH a[href]::after` to print escalation URLs, and `@media (max-width: 480px)` collapsing the bilingual grid to single column with form-input font-size pinned at 16px. Tasks 1 and 2 each exited 0. `verify-no-mic.ps1` exited 0.
  </what-built>
  <how-to-verify>
    Francisco must run two walkthroughs against the live `index.html`. Both use `?mock=1` so no API tokens are spent.

    **A. Print-preview checklist (10 steps, verbatim from 03-RESEARCH.md Section 4)**

    1. Open `index.html?mock=1` in desktop Chrome (file:// or the deployed GitHub Pages URL).
    2. Step through key entry -> intake -> Generate -> land on the rendered cheat sheet (all eight blocks visible on screen, except Block G which is screen-hidden).
    3. Hit Ctrl+P (or click the in-page Print button).
    4. In the print dialog: destination = `Save as PDF`.
    5. Paper size = `A4`.
    6. Margins = `Default` (Chrome respects the `@page` value).
    7. **`Background graphics` checkbox is TICKED** so the block borders and disclaimer outline print.
    8. Visually scan the preview pages:
       - Page count is 1 or 2.
       - No `.cheat-sheet-block` is split across a page boundary.
       - No button, no form control, no key-entry strip, no raw JSON panel is visible.
       - The disclaimer sits at the top of page 1, with a black border, NOT sticky-overlapping content.
       - Block G shows four labelled blank lines (`Officer name:`, `Next step:`, `Date:`, `Reference number:`) with monospace underscores.
       - Block H escalation links print their URLs in parentheses after the link text.
    9. Save as PDF, open the PDF, confirm the visual stays correct after PDF round-trip.
    10. Report PASS or describe the specific block / control / page break that misbehaved.

    **B. Mobile walkthrough (Chrome devtools iPhone 15 Pro emulation, soft-pass)**

    1. Open `index.html?mock=1` in desktop Chrome.
    2. Open DevTools (F12).
    3. Click the Device Toolbar icon (Ctrl+Shift+M).
    4. Choose `iPhone 15 Pro` from the device dropdown.
    5. Reload the page.
    6. Walk through: paste key (or use the saved key), reach the intake screen.
       - Confirm form inputs are at a readable font size (no iOS-style auto-zoom appearance).
    7. Fill intake (any valid combination) and submit.
    8. Land on the result screen with all eight blocks.
       - Confirm `.bilingual-row` rows in Block B, Block C, Block D, Block E render as a single vertical column (DE on top, EN below).
       - Confirm no horizontal scrolling occurs.
       - Confirm the disclaimer is visible and the block borders are intact.
       - Confirm the Print button and the Ctrl+P hint are reachable without overflow.
    9. Tap the Print button. Confirm a print dialog opens (devtools forwards this to the host's Chrome print dialog; visually confirm it appears).
    10. Report PASS or describe the specific layout issue (overflow, font-size, broken collapse).

    The acceptance question Francisco answers: **does the printed A4 look like a usable cheat sheet, AND does the phone-emulation view stay single-column with readable type?** If both are yes, Plan 03-02 is approved. If one fails, describe which step and Plan 03-02 ships a fix before Plan 03-03 starts.

    REQUIREMENTS.md PRINT-02 stays listed as `Phase 3 soft pass, Phase 5 hard pass on real iPhone` even on success; this checkpoint does NOT promote PRINT-02 to fully-verified.
  </how-to-verify>
  <resume-signal>
    Type `approved` if both walkthrough A and walkthrough B pass. Otherwise describe the specific failure (which checklist step, what was wrong) so the executor can patch the print or mobile CSS before Plan 03-03 starts.
  </resume-signal>
</task>

</tasks>

<verification>
- Tasks 1 and 2 each exit 0 on their automated verify.
- `verify-no-mic.ps1` exits 0 (TRUST-04 lifetime guardrail).
- Task 3 returns `approved`.
- CSP meta tag is unchanged (no font-src or style-src widening).
- No `@import`, no `@font-face`, no external `url()` font loads.
- A re-run of Plan 03-01's verify suite still passes (the cheat-sheet DOM was not touched by this plan).
</verification>

<success_criteria>
- PRINT-01: print preview against `?mock=1` produces a 1-2 page A4 layout with no block split across a page break (verified in Task 3 walkthrough A).
- PRINT-02: phone-emulation walkthrough shows a single-column readable layout (soft pass; hard pass deferred to Phase 5). Documented in PLAN frontmatter so the requirement isn't ticked off too early.
- PRINT-03: print preview shows ONLY `#cheatSheet` content (plus disclaimer); every non-cheat-sheet UI is hidden.
- TRUST-04 carry-forward: `verify-no-mic.ps1` exits 0.
- L-07 carry-forward: CSP unchanged, no CDN, no new font, no @import.
</success_criteria>

<output>
After Task 3 returns `approved`, create `.planning/phases/03-cheat-sheet-rendering-print-mobile-onboarding-privacy/03-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- The exact `@media print` selector list shipped, including the `#onboarding` / `#privacy` forward-compatible entries.
- The mobile breakpoint locked at `<= 480px` and the 16px form-input rule.
- Francisco's checklist outcome for walkthrough A (the 10-step print preview) and walkthrough B (iPhone 15 Pro emulation).
- Note that PRINT-02 remains in `Phase 3 soft pass, Phase 5 hard pass on real iPhone` status.
- Any deviations from this plan (e.g. additional hide selectors added because some Phase 2 element was overlooked).
- Items to carry into Plan 03-03 (onboarding + privacy + TRUST-05 housekeeping). The `#onboarding` and `#privacy` selectors are already wired into the print hide rule, so Plan 03-03 only adds the DOM, not new print rules.
</output>
