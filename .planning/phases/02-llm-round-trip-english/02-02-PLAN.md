---
phase: 02-llm-round-trip-english
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - index.html
  - README.md
autonomous: false
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
tags: [llm, anthropic, structured-output, mock-fixture, error-handling, phase-2]
decisions:
  - "LLM call shape: POST https://api.anthropic.com/v1/messages with model 'claude-haiku-4-5', max_tokens 2500, temperature 0.3, system prompt verbatim from 02-RESEARCH.md Section 1, user message built from appState.lastIntake, output_config.format.type 'json_schema' with CHEAT_SHEET_SCHEMA. Headers: x-api-key, anthropic-version: 2023-06-01, anthropic-dangerous-direct-browser-access: true, content-type: application/json."
  - "Use output_config.format (Anthropic structured output, GA on claude-haiku-4-5) per 02-RESEARCH.md Section 2 Option A. Tool-use forced-call is documented as a fallback only and is NOT implemented in Plan 02."
  - "JSON schema field names are descriptive (Francisco's locked decision): germanLine, englishGloss, germanQuestion, affirmativeGerman, affirmativeEnglish, negativeGerman, negativeEnglish, article, germanWord, englishTranslation. Plan structure exactly as in 02-RESEARCH.md Section 3."
  - "No prompt caching in v1 (system prompt + schema ~1300 tokens, under the 4096-token Haiku 4.5 cache minimum; caching would silently no-op). Documented in code comment."
  - "Mock flag: ?mock=1 returns MOCK_CHEAT_SHEET after an 800ms setTimeout (simulates latency). ?mock=broken returns MOCK_CHEAT_SHEET_BROKEN as a string the parser will fail on, exercising the parse-error branch. No other mock values."
  - "Loading screen cycles 5 reassurance messages every 4 seconds, stopping on message 5 if generation exceeds 20s. Per 02-RESEARCH.md Section 5 'Loading state copy'."
  - "Result screen renders labelled block sections (B/C/E/F) via textContent only, plus a <details><summary>Raw JSON</summary><pre> dump at the bottom. Two buttons: 'New cheat sheet' and 'Back to intake'. Per 02-RESEARCH.md Section 6."
  - "Error panel renders five classes ('auth', 'rate-limit', 'server', 'parse', 'network') with copy verbatim from 02-RESEARCH.md Section 7, with one wording adjustment: all UI-visible mentions of 'Anthropic' are replaced with 'the AI service' (Francisco's locked decision). The collapsible technical details section may still display the literal HTTP status code and response body text returned by Anthropic."
  - "Tolerant parser (stripFences) runs before JSON.parse defensively. If output_config.format ever silently fails, this catches the most common drift (markdown fences)."
  - "Generate button click swaps screen to 'generating' IMMEDIATELY (before the fetch promise resolves), so a double-click cannot trigger two calls."
  - "appState.lastIntake is set BEFORE the fetch and survives all error paths. 'Try again' resends the same payload. 'Back to intake' returns to the intake form with the same fields populated."
  - "Result screen is intentionally throwaway: Phase 3 deletes most of it and replaces with the printable Block A-H cheat sheet."
  - "Human-verify checkpoint at end of plan: Francisco runs a real call against claude-haiku-4-5 with his personal Anthropic key and confirms the German output is sensible. This validates the load-bearing assumption of Phase 2 before Phase 3 plans against the schema."

must_haves:
  truths:
    - "Constants SYSTEM_PROMPT, CHEAT_SHEET_SCHEMA, ANTHROPIC_API exist at the top of the inline <script> block, after BLOCK_H_FOOTER and before appState"
    - "MOCK_CHEAT_SHEET object validates against CHEAT_SHEET_SCHEMA (4 required keys, all field names match Section 3)"
    - "?mock=1 in the URL routes the Generate handler to the mock fixture, no Anthropic fetch happens"
    - "?mock=broken in the URL routes the Generate handler to a malformed-JSON path that lands on the 'parse' error class"
    - "On Generate click, the screen immediately swaps to 'generating' with cycling reassurance messages"
    - "On 200+valid JSON, the screen swaps to 'result' and renders Block B (userGoalSentence + openingScript), Block C (questions + affirmative + negative), Block E (article + word + translation), Block F (checklist items)"
    - "On 401, the error panel shows the 'auth' copy with three buttons: Update key, Try again, Back to intake"
    - "On 429, the error panel shows the 'rate-limit' copy with two buttons: Try again, Back to intake"
    - "On HTTP 4xx other or 5xx, the error panel shows the 'server' copy with two buttons"
    - "On 200 with malformed JSON or missing required schema keys, the error panel shows the 'parse' copy"
    - "On fetch network error (caught exception), the error panel shows the 'network' copy"
    - "Update key (only on the 'auth' error) routes to 'key-entry' with appState.lastIntake preserved in memory; on Save key, the user lands back on 'intake' with the form re-populated"
    - "README documents ?mock=1 and ?mock=broken plus the approximate per-call cost"
    - "All LLM-returned text is rendered via textContent only (no innerHTML), including the raw JSON <pre>"
    - "The API key is never written to console, never written to the DOM, never serialised into a non-Anthropic-bound payload"
    - "verify-no-mic.ps1 still exits 0"
  artifacts:
    - path: "index.html"
      provides: "ANTHROPIC_API config object, SYSTEM_PROMPT const, CHEAT_SHEET_SCHEMA const, MOCK_CHEAT_SHEET fixture, MOCK_CHEAT_SHEET_BROKEN string, generating/result/error sections, callAnthropic / buildRequestBody / parseAndValidateResponse / stripFences / cycleLoadingMessages helpers"
      contains: "claude-haiku-4-5"
    - path: "README.md"
      provides: "?mock=1 and ?mock=broken documentation, per-call cost estimate, model id note"
      contains: "?mock=1"
  key_links:
    - from: "Generate button click handler (from Plan 01)"
      to: "callAnthropic(appState.lastIntake, appState.apiKey)"
      via: "Plan 01 left a stub console.warn at this site; Plan 02 replaces the body with: screen swap to 'generating', start cycleLoadingMessages, await callAnthropic, route to result/error"
      pattern: "callAnthropic\\("
    - from: "callAnthropic"
      to: "https://api.anthropic.com/v1/messages"
      via: "fetch with output_config.format json_schema body, three required headers"
      pattern: "fetch\\(['\"]https://api\\.anthropic\\.com/v1/messages"
    - from: "parseAndValidateResponse"
      to: "appState.cheatSheet"
      via: "JSON.parse(stripFences(response.content[0].text)) -> object with blockB/blockC/blockE/blockF -> set on appState"
      pattern: "JSON\\.parse"
    - from: "URL ?mock flag"
      to: "callAnthropic mock branch"
      via: "new URLSearchParams(window.location.search).get('mock') checked at start of callAnthropic"
      pattern: "URLSearchParams"
---

<objective>
Wire the live LLM round-trip end to end on top of the Plan 01 intake form. Add the system prompt, the locked JSON schema, the Anthropic call, the structured-output request shape, the generating screen with cycling reassurance copy, the result screen showing the parsed blocks, the error panel with five classes, the mock-fixture URL flag, and the README update. Close Phase 2 with a human-verify checkpoint where Francisco runs one real generation against his personal Anthropic key and confirms the German is sensible.

Purpose: Validate the load-bearing assumption of Phase 2 - that claude-haiku-4-5 with the locked system prompt and schema produces a usable cheat sheet - before Phase 3 builds the printable view against the same schema.

Output: Modified `index.html` (constants, helpers, screens, error panel) and `README.md` (mock flag + cost note). The Plan 01 stub in the Generate handler is replaced with the real flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-llm-round-trip-english/02-CONTEXT.md
@.planning/phases/02-llm-round-trip-english/02-RESEARCH.md
@.planning/phases/02-llm-round-trip-english/02-01-PLAN.md
@.planning/phases/00-cors-and-provider-spike/00-01-SUMMARY.md
@.planning/phases/01-skeleton-state-key-constants/01-01-SUMMARY.md
@index.html
@README.md
@CLAUDE.md

<interfaces>
<!-- Plan 01 (sibling) extended appState with: lastIntake, cheatSheet, errorClass, errorDetail, consentGiven. -->
<!-- Plan 01 left a stub at the Generate click handler that console.warns. Plan 02 replaces that body. -->
<!-- Phase 1 ships maskKey, setState, render. Plan 02 extends render() to implement the 'generating', 'result', 'error' branches that Plan 01 stubbed out with renderUnimplemented. -->
<!-- Anthropic Messages API (verified Phase 0):
       POST https://api.anthropic.com/v1/messages
       Headers: x-api-key, anthropic-version: 2023-06-01,
                anthropic-dangerous-direct-browser-access: true,
                content-type: application/json
       Body: { model, max_tokens, temperature, system, messages, output_config }
       Response: { content: [{ type: 'text', text: '<JSON string>' }], ... }
       The text field is the JSON the model produced. JSON.parse it.
-->
<!-- CHEAT_SHEET_SCHEMA shape (verbatim from 02-RESEARCH.md Section 3, locked descriptive field names):
     {
       blockB: {
         userGoalSentence: string,
         userGoalGloss: string,
         openingScript: Array<{ germanLine, englishGloss }>   (3..5 items)
       },
       blockC: Array<{                                          (4..6 items)
         germanQuestion, englishGloss,
         affirmativeGerman, affirmativeEnglish,
         negativeGerman, negativeEnglish
       }>,
       blockE: Array<{ article: 'der'|'die'|'das', germanWord, englishTranslation }>  (6..12 items)
       blockF: Array<string>                                    (5..10 items)
     }
-->
<!-- output_config request body fragment:
     "output_config": { "format": { "type": "json_schema", "schema": <CHEAT_SHEET_SCHEMA> } }
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ANTHROPIC_API, SYSTEM_PROMPT, CHEAT_SHEET_SCHEMA, MOCK_CHEAT_SHEET, MOCK_CHEAT_SHEET_BROKEN constants</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/02-llm-round-trip-english/02-RESEARCH.md Section 1 (system prompt verbatim), Section 3 (schema verbatim), Section 9 (code location)
    - index.html lines 200 to 280 (current constants block, ending with BLOCK_H_FOOTER and starting appState)
  </read_first>
  <action>
    Insert four new constants into the inline `<script>` block, immediately after `BLOCK_H_FOOTER` and before the `appState` declaration. Use the 70-dash banner-comment style from Phase 1 for each. Order:

    1. **ANTHROPIC_API** - configuration object. Fields per 02-RESEARCH.md Section 9:
       ```
       endpoint: 'https://api.anthropic.com/v1/messages'
       model: 'claude-haiku-4-5'
       apiVersion: '2023-06-01'
       maxTokens: 2500
       temperature: 0.3
       ```
       Add a code comment above the object explaining: prompt caching is intentionally not enabled in v1 because claude-haiku-4-5 requires 4096+ tokens of stable prefix and our system prompt plus schema is only about 1300 tokens; caching would silently no-op. Also document that a 404 on this endpoint usually means the model id has been retired (Phase 0 lesson learned) and the fix is a one-line edit to `ANTHROPIC_API.model`.

    2. **SYSTEM_PROMPT** - a single template-literal string `const SYSTEM_PROMPT = `...`;`. Copy the text from 02-RESEARCH.md Section 1 (the block beginning with `You are a preparation aid for a single phone call to the Migrationsamt of canton Zürich...` and ending with `Reply in JSON only. The calling application enforces the schema via the API's structured-output feature; your response must validate against it.`) VERBATIM, including the XML-style fences `<role_boundaries>`, `<forbidden_behaviour>`, `<generation_scope>`, `<language_anchors>`, `<edge_cases>`. Do not paraphrase. Do not reformat. Do not collapse blank lines.

       Banner comment above the constant: explain that this prompt is the load-bearing design artefact of Phase 2 and the expected tuning trigger is the Phase 5 pilot debrief (per STATE.md Open Questions).

    3. **CHEAT_SHEET_SCHEMA** - the JSON-schema-shaped JS object from 02-RESEARCH.md Section 3. Copy verbatim including comments. The structure is:
       ```javascript
       const CHEAT_SHEET_SCHEMA = {
         type: 'object',
         properties: {
           blockB: { ... },  // userGoalSentence, userGoalGloss, openingScript[3..5]{germanLine, englishGloss}
           blockC: { ... },  // 4..6 entries of germanQuestion, englishGloss, affirmativeGerman/English, negativeGerman/English
           blockE: { ... },  // 6..12 entries of article (enum der/die/das), germanWord, englishTranslation
           blockF: { ... },  // 5..10 strings
         },
         required: ['blockB', 'blockC', 'blockE', 'blockF'],
         additionalProperties: false,
       };
       ```
       Copy the full structure with `minItems`, `maxItems`, `required`, `additionalProperties` exactly as in Section 3. Banner comment notes: Phase 3 rendering reads the SAME shape; any field rename here requires updating Phase 3 simultaneously.

    4. **MOCK_CHEAT_SHEET** - a hardcoded JS object matching the schema, used for `?mock=1`. Author one fixture covering all four blocks. Suggested content (Francisco can refine; the goal is something that validates):
       - blockB.userGoalSentence: a one-sentence Hochdeutsch statement, e.g. `Guten Tag, ich rufe an, um den Status meiner B-Bewilligungsverlängerung zu erfragen.`
       - blockB.userGoalGloss: `Hello, I am calling to check the status of my B permit renewal.`
       - blockB.openingScript: 4 entries with germanLine + englishGloss. Include the Hochdeutsch-request sentence as the second entry (from the HOCHDEUTSCH_REQUEST constant: `Können wir bitte auf Hochdeutsch sprechen? Mein Schweizerdeutsch ist nicht so gut.`).
       - blockC: 4 entries covering common renewal questions (e.g. "Haben Sie Ihre AHV-Nummer?", "Wann läuft Ihre aktuelle Bewilligung ab?", "Sind Sie noch bei derselben Arbeitsstelle?", "Hat sich Ihre Adresse geändert?"), each with all six fields populated.
       - blockE: 6 entries (article + germanWord + englishTranslation), e.g. {die, Bewilligung, permit}, {der, Antrag, application}, {die, Verlängerung, renewal}, {das, Dokument, document}, {die, Frist, deadline}, {die, Aufenthaltsbewilligung, residence permit}.
       - blockF: 5 checklist items in English.

       This fixture is used both for `?mock=1` runtime and as a structural integrity check; keep it readable. Banner comment: this object validates against CHEAT_SHEET_SCHEMA by construction and is the source of truth for Phase 3 rendering iteration without burning API tokens.

    5. **MOCK_CHEAT_SHEET_BROKEN** - a string constant containing JSON-shaped text that JSON.parse will reject. Suggested value: `'{"blockB": {"userGoalSentence": "incomplete'` (unterminated string). Use this with `?mock=broken` to exercise the parse-error branch.

    Section 9 of the research file also recommends helper functions live alongside the constants. Defer those to Task 2; in this task, only the five constants and their banner comments land.

    Hard constraints:
    - No `innerHTML` introduced.
    - SYSTEM_PROMPT is one template literal; do not break it into pieces.
    - The angle-bracket fences `<role_boundaries>` etc. inside SYSTEM_PROMPT are LITERAL string content; they are not HTML tags. Authoring inside a `<script>` block, this is safe because the template literal is JS, not HTML parsing context. Re-check by opening the file in Chrome and confirming no DOM nodes named `role_boundaries` exist.
    - No em or en dashes anywhere.
    - The literal `claude-haiku-4-5` appears exactly once (in ANTHROPIC_API.model). The retired id `claude-3-5-haiku-latest` does NOT appear in index.html anywhere.

    Re-run verify-no-mic.ps1 mentally: the constants block contains no `getUserMedia` reference.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "node -e \"const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const checks={hasAnthropicApi:/const\\s+ANTHROPIC_API\\s*=/.test(s),hasEndpoint:/endpoint:\\s*'https:\\/\\/api\\.anthropic\\.com\\/v1\\/messages'/.test(s),hasModel:/model:\\s*'claude-haiku-4-5'/.test(s),hasApiVersion:/apiVersion:\\s*'2023-06-01'/.test(s),hasMaxTokens:/maxTokens:\\s*2500/.test(s),hasTemperature:/temperature:\\s*0\\.3/.test(s),hasSystemPrompt:/const\\s+SYSTEM_PROMPT\\s*=\\s*`/.test(s),systemHasRoleBoundaries:/<role_boundaries>/.test(s),systemHasForbidden:/<forbidden_behaviour>/.test(s),systemHasGenerationScope:/<generation_scope>/.test(s),systemHasLanguageAnchors:/<language_anchors>/.test(s),systemHasEdgeCases:/<edge_cases>/.test(s),systemForbidsContact:/contact details/i.test(s),systemForbidsEligibility:/permit-eligibility/i.test(s),systemForbidsStrategic:/strategic legal advice/i.test(s),systemReplyJsonOnly:/Reply in JSON only/.test(s),hasSchemaConst:/const\\s+CHEAT_SHEET_SCHEMA\\s*=/.test(s),schemaRequired:/required:\\s*\\[\\s*'blockB'\\s*,\\s*'blockC'\\s*,\\s*'blockE'\\s*,\\s*'blockF'/.test(s),schemaBlockB:/blockB:\\s*\\{/.test(s),schemaUserGoalSentence:/userGoalSentence/.test(s),schemaUserGoalGloss:/userGoalGloss/.test(s),schemaOpeningScript:/openingScript/.test(s),schemaGermanLine:/germanLine/.test(s),schemaEnglishGloss:/englishGloss/.test(s),schemaGermanQuestion:/germanQuestion/.test(s),schemaAffirmativeGerman:/affirmativeGerman/.test(s),schemaAffirmativeEnglish:/affirmativeEnglish/.test(s),schemaNegativeGerman:/negativeGerman/.test(s),schemaNegativeEnglish:/negativeEnglish/.test(s),schemaArticleEnum:/enum:\\s*\\[\\s*'der'\\s*,\\s*'die'\\s*,\\s*'das'/.test(s),schemaGermanWord:/germanWord/.test(s),schemaEnglishTranslation:/englishTranslation/.test(s),hasMockFixture:/const\\s+MOCK_CHEAT_SHEET\\s*=/.test(s),mockHasAllFour:/blockB[\\s\\S]*blockC[\\s\\S]*blockE[\\s\\S]*blockF/.test(s),mockBroken:/const\\s+MOCK_CHEAT_SHEET_BROKEN\\s*=/.test(s),noRetiredModel:!/claude-3-5-haiku/.test(s),noInnerHtml:!/\\.innerHTML\\s*=/.test(s),noGetUserMedia:!/getUserMedia/.test(s),noEmDash:!s.includes('—'),noEnDash:!s.includes('–')};let fail=0;for(const k in checks){console.log(k,checks[k]);if(!checks[k])fail++;}process.exit(fail===0?0:1);\""</automated>
    <acceptance_criteria>
      - All 38 named checks pass.
      - SYSTEM_PROMPT contains the five XML fences as literal strings.
      - CHEAT_SHEET_SCHEMA has all the descriptive field names locked by Francisco.
      - The literal `claude-3-5-haiku-latest` does NOT appear anywhere in the file.
      - MOCK_CHEAT_SHEET and MOCK_CHEAT_SHEET_BROKEN both exist as top-level consts.
    </acceptance_criteria>
  </verify>
  <done>
    The five new constants are present, in order, with banner comments. The system prompt, schema, and mock fixture are the source of truth for Tasks 2 through 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add helper functions (buildRequestBody, callAnthropic, parseAndValidateResponse, stripFences, cycleLoadingMessages) and the mock-flag branch</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/02-llm-round-trip-english/02-RESEARCH.md Section 2 (output_config.format), Section 4 (params), Section 9 (helper grouping), Section 10 (mock flag), Section 7 (error class mapping)
    - index.html (post-Task-1) - locate the appState / setState / render block and the post-Plan-01 Generate handler stub
  </read_first>
  <action>
    Add a new helpers block in the inline `<script>`, grouped together below `appState`/`setState`/`render` and above the event handlers. Banner comment: `LLM ROUND-TRIP HELPERS (Phase 2 Plan 02)`.

    Functions to add, in order:

    1. **`stripFences(text)`**: trims the string; if it starts with ` ```json `, drops the first 7 chars; else if it starts with ` ``` `, drops the first 3; if it ends with ` ``` `, drops the last 3; returns the trimmed result. Three-line defensive utility per 02-RESEARCH.md Risk 2.

    2. **`buildRequestBody(intake)`**: composes the messages array. Returns an object shaped exactly:
       ```
       {
         model: ANTHROPIC_API.model,
         max_tokens: ANTHROPIC_API.maxTokens,
         temperature: ANTHROPIC_API.temperature,
         system: SYSTEM_PROMPT,
         messages: [{ role: 'user', content: buildUserMessage(intake) }],
         output_config: { format: { type: 'json_schema', schema: CHEAT_SHEET_SCHEMA } },
       }
       ```
       `buildUserMessage(intake)` is a small inline helper that composes a plain-English description of the intake. Suggested shape:
       ```
       My permit type is: {permit}.
       My reason for calling: {reason}.
       Case reference number: {caseRef or 'none provided'}.
       AHV number: {ahv or 'none provided'}.
       Appointment date: {appointmentDate or 'none provided'}.
       My situation:
       {situation}
       ```
       Keep it deterministic. Do not embed any HTML or markdown. Pass values through `String(...)` defensively (the intake fields are user-typed and should already be strings, but defense in depth is cheap).

    3. **`async function callAnthropic(intake, apiKey)`**. Returns `{ ok, data, status, bodyText, errorClass, message }` (only the fields relevant to the outcome are populated; the rest are undefined).
       - **Mock branch (top of function):**
         ```
         const mockParam = new URLSearchParams(window.location.search).get('mock');
         if (mockParam === '1') {
           await new Promise(r => setTimeout(r, 800));   // simulate the 10-20s wait in miniature
           return { ok: true, data: MOCK_CHEAT_SHEET };
         }
         if (mockParam === 'broken') {
           await new Promise(r => setTimeout(r, 400));
           // Return a shape that mimics a real Anthropic response with a malformed JSON string
           return { ok: true, data: null, rawText: MOCK_CHEAT_SHEET_BROKEN, mockBroken: true };
         }
         ```
         When `mockBroken` is true, the calling code (Task 3) must route through the same parse logic as a real response, where JSON.parse will throw and the error class becomes 'parse'.
       - **Real call:**
         - `try { const response = await fetch(ANTHROPIC_API.endpoint, { method: 'POST', headers: { 'x-api-key': apiKey, 'anthropic-version': ANTHROPIC_API.apiVersion, 'anthropic-dangerous-direct-browser-access': 'true', 'content-type': 'application/json' }, body: JSON.stringify(buildRequestBody(intake)) }); } catch (err) { return { ok: false, errorClass: 'network', message: err && err.message ? err.message : String(err) }; }`
         - If `response.ok === false`: read `await response.text()` for the bodyText, map status to errorClass: 401 -> 'auth', 429 -> 'rate-limit', any other 4xx or any 5xx -> 'server'. Return `{ ok: false, errorClass, status: response.status, bodyText }`.
         - If `response.ok === true`: parse JSON of the wrapper (Anthropic envelope) with `const envelope = await response.json();` inside its own try/catch (a malformed envelope is rare but possible -> errorClass 'parse'). Extract `envelope.content[0].text`. If `content` is missing or `content[0].type !== 'text'`, treat as parse error. Otherwise return `{ ok: true, data: null, rawText: envelope.content[0].text }` and let the caller hand it to `parseAndValidateResponse`.
       - The function MUST NOT log `apiKey`. The function MUST NOT log the request body. Use `try/catch` but do NOT `console.error` the full request object. A short `console.warn('LLM call failed:', status)` is acceptable for development; passing the API key is not.

    4. **`parseAndValidateResponse(rawTextOrObject)`**. Two callers:
       - The mock-success branch passes the fixture object directly. If the input is an object (typeof === 'object' && input !== null), treat it as already-parsed and skip JSON.parse.
       - The real-success and mock-broken branches pass a string. Run `JSON.parse(stripFences(input))` inside try/catch. On throw, return `{ ok: false, errorClass: 'parse', message: err.message, rawText: input.slice(0, 2000) }`.
       - After parse, validate against the schema's `required` fields shallowly: confirm the result is a non-null object and has each of `blockB`, `blockC`, `blockE`, `blockF` as own properties. Confirm `blockB` is an object with `userGoalSentence`, `userGoalGloss`, `openingScript`. Confirm `blockC`, `blockE` are arrays. Confirm `blockF` is an array of strings. On any failure, return `{ ok: false, errorClass: 'parse', message: 'response did not match the expected schema', rawText: typeof input === 'string' ? input.slice(0, 2000) : JSON.stringify(input).slice(0, 2000) }`.
       - On success return `{ ok: true, cheatSheet: parsed }`.

    5. **`cycleLoadingMessages()`**. Reads the existing `#loadingMessage` element (Task 3 introduces it). Cycles through an array of 5 strings (verbatim from 02-RESEARCH.md Section 5):
       1. `Building your cheat sheet now. This usually takes 10 to 20 seconds.`
       2. `Translating your situation into formal German.`
       3. `Picking the questions the officer is most likely to ask.`
       4. `Compiling vocabulary and your prep checklist.`
       5. `Almost there. Final pass on the German.`
       Implementation: a module-level variable `let loadingTimerId = null;`. On start, set `#loadingMessage.textContent = messages[0]`, then `setInterval` every 4000ms advancing the index (clamped at 4, i.e. message 5 sticks). Provide a paired `stopLoadingMessages()` that `clearInterval(loadingTimerId); loadingTimerId = null;`.

    Hard constraints:
    - All writes to the DOM via `textContent` only.
    - The API key is passed as a function argument, never read from the DOM in `callAnthropic`. Never logged.
    - The request body is never logged.
    - No `eval`. No `new Function`. No `innerHTML`.
    - Add inline comments explaining each helper in plain language (Francisco is a beginner).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "node -e \"const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const checks={hasStripFences:/function\\s+stripFences\\s*\\(/.test(s),hasBuildRequestBody:/function\\s+buildRequestBody\\s*\\(/.test(s),hasBuildUserMessage:/function\\s+buildUserMessage\\s*\\(/.test(s),hasCallAnthropic:/async\\s+function\\s+callAnthropic\\s*\\(/.test(s),hasParseValidate:/function\\s+parseAndValidateResponse\\s*\\(/.test(s),hasCycleLoading:/function\\s+cycleLoadingMessages\\s*\\(/.test(s),hasStopLoading:/function\\s+stopLoadingMessages\\s*\\(/.test(s),usesOutputConfig:/output_config:\\s*\\{[^}]*format:\\s*\\{[^}]*type:\\s*'json_schema'/.test(s),sendsApiKeyHeader:/'x-api-key':\\s*apiKey/.test(s),sendsVersion:/'anthropic-version':\\s*ANTHROPIC_API\\.apiVersion/.test(s),sendsBrowserAccess:/'anthropic-dangerous-direct-browser-access':\\s*'true'/.test(s),mockOneBranch:/mockParam\\s*===\\s*'1'/.test(s),mockBrokenBranch:/mockParam\\s*===\\s*'broken'/.test(s),mapStatus401:/(401[^a-z]*'auth')|('auth'[\\s\\S]*?401)|(status\\s*===\\s*401)/.test(s),mapStatus429:/(429[^a-z]*'rate-limit')|('rate-limit'[\\s\\S]*?429)|(status\\s*===\\s*429)/.test(s),mapNetwork:/errorClass:\\s*'network'/.test(s),mapParse:/errorClass:\\s*'parse'/.test(s),loadingMsgArray5:/Building your cheat sheet now[\\s\\S]*Almost there\\. Final pass on the German/.test(s),noLogApiKey:!/console\\.\\w+\\([^)]*apiKey/.test(s),noLogRequestBody:!/console\\.\\w+\\([^)]*JSON\\.stringify\\(.*buildRequest/.test(s),noInnerHtml:!/\\.innerHTML\\s*=/.test(s),noEval:!/\\beval\\s*\\(/.test(s),noNewFunction:!/new\\s+Function\\s*\\(/.test(s),noGetUserMedia:!/getUserMedia/.test(s),noEmDash:!s.includes('—'),noEnDash:!s.includes('–')};let fail=0;for(const k in checks){console.log(k,checks[k]);if(!checks[k])fail++;}process.exit(fail===0?0:1);\""</automated>
    <acceptance_criteria>
      - All 26 checks pass.
      - The request body uses `output_config.format` with `type: 'json_schema'` (Option A), not tool-use forced-call (Option B).
      - All three required Anthropic headers (`x-api-key`, `anthropic-version`, `anthropic-dangerous-direct-browser-access: 'true'`) are present in the fetch call.
      - 401 maps to 'auth', 429 maps to 'rate-limit', network throws map to 'network'.
      - The API key is not logged. The request body is not logged.
    </acceptance_criteria>
  </verify>
  <done>
    Five helper functions are defined and ready. The mock-flag branch short-circuits the network call. The real-call branch maps HTTP outcomes to error classes per Section 7.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add generating, result, and error screen DOM + extend render() to handle all three</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/02-llm-round-trip-english/02-RESEARCH.md Section 5 (loading copy), Section 6 (result layout), Section 7 (error copy verbatim)
    - index.html post-Task-2 - the render() switch has stub branches for generating/result/error from Plan 01
  </read_first>
  <action>
    Add three new `<section>` elements inside `<main>`, immediately after `#intake`, each `hidden` by default.

    1. **`<section id="generating" hidden>`**:
       - H2: `Generating your cheat sheet`
       - `<p id="loadingMessage" role="status" aria-live="polite"></p>` (populated by `cycleLoadingMessages`)
       - A simple `<progress>` element (no `value`/`max` -> indeterminate progress bar). No spinner library, no CSS animation library.
       - Helper paragraph: `This usually takes 10 to 20 seconds. Please keep this tab open.`

    2. **`<section id="result" hidden>`**:
       - H2: `Your cheat sheet (Phase 2 raw preview)`
       - Helper paragraph: `This is the raw structured output. In a later step we will format it as a printable cheat sheet. For now, look through it to check the German makes sense and the schema is right.`
       - Four labelled subsections, each with an H3 and a content container:
         - `<div id="resultBlockB">` (Block B - Opening script)
         - `<div id="resultBlockC">` (Block C - Likely officer questions)
         - `<div id="resultBlockE">` (Block E - Vocabulary)
         - `<div id="resultBlockF">` (Block F - Prep checklist)
       - A `<details>` containing `<summary>Raw JSON for debugging</summary><pre id="resultRawJson"></pre>` at the bottom.
       - Two buttons: `<button id="newCheatSheetButton">New cheat sheet</button>` and `<button id="backToIntakeFromResultButton">Back to intake</button>`.

    3. **`<section id="errorPanel" hidden>`**:
       - `<h2 id="errorHeading"></h2>` (populated dynamically)
       - `<p id="errorBody"></p>`
       - Three buttons, two always-visible and one conditional:
         - `<button id="errorTryAgainButton">Try again</button>`
         - `<button id="errorBackToIntakeButton">Back to intake</button>`
         - `<button id="errorUpdateKeyButton" hidden>Update key</button>` (the auth-class branch in render() unhides this)
       - `<details><summary>Show technical details</summary><pre id="errorDetailPre"></pre></details>` (populated from `appState.errorDetail`).

    Then extend `render()` to fully implement the three branches that were stubbed by `renderUnimplemented` in Plan 01:

    - **`'generating'`**: hide every other section; show `#generating`; call `cycleLoadingMessages()` (which starts the interval if not already running).
    - **`'result'`**: stop loading messages; hide all other sections; show `#result`; render the four blocks via dedicated helpers `renderBlockB(cheatSheet.blockB)`, `renderBlockC(cheatSheet.blockC)`, `renderBlockE(cheatSheet.blockE)`, `renderBlockF(cheatSheet.blockF)`; set `#resultRawJson.textContent = JSON.stringify(appState.cheatSheet, null, 2)`.
    - **`'error'`**: stop loading messages; hide all other sections; show `#errorPanel`; switch on `appState.errorClass` and write heading + body via `textContent` per the copy table below. Toggle `#errorUpdateKeyButton.hidden` based on `errorClass === 'auth'`. Populate `#errorDetailPre.textContent = JSON.stringify(appState.errorDetail || {}, null, 2)`.

    **Render helpers** (each writes via textContent only; create child elements with `document.createElement`, never `innerHTML`):

    - `renderBlockB(block)`: clears `#resultBlockB`, appends a paragraph `Goal (DE): <userGoalSentence>` and `Goal (EN): <userGoalGloss>`, then an ordered list of openingScript items each formatted as `DE: <germanLine> / EN: <englishGloss>`.
    - `renderBlockC(items)`: clears `#resultBlockC`, appends an ordered list. Each list item contains a paragraph for the question (DE + EN), a Yes line (affirmativeGerman / affirmativeEnglish), and a No line (negativeGerman / negativeEnglish).
    - `renderBlockE(items)`: clears `#resultBlockE`, appends an unordered list of `<article> <germanWord> - <englishTranslation>` (use a regular hyphen, NOT an em or en dash).
    - `renderBlockF(items)`: clears `#resultBlockF`, appends an unordered list of plain string entries.

    **Error copy table** (UI strings, all written via textContent):

    | errorClass | Heading | Body |
    |------------|---------|------|
    | auth | `Your API key did not work` | `The AI service rejected your saved API key. It might be wrong, expired, or revoked. You can paste a new key and try again, or go back to the intake.` |
    | rate-limit | `The AI service is rate-limiting your account right now` | `Your account has hit a temporary rate limit on the AI service. Wait a minute or two and try again, or change your intake and come back.` |
    | server | `The AI service is having a problem` | `The AI service returned an error from their side. This usually clears up by itself within a few minutes. Try again, or come back later.` |
    | parse | `Something went wrong reading the response` | `The AI service answered, but the response did not match the expected shape. This is rare. Trying again usually fixes it. If it keeps happening, this is a bug worth reporting.` |
    | network | `Could not reach the AI service` | `Your browser could not connect to the AI service. Check your internet connection and try again.` |

    (Note: all five strings replace the literal `Anthropic` from Section 7 with `the AI service` / `The AI service`, per Francisco's locked decision.)

    **Wire all five new buttons:**

    - `#newCheatSheetButton`: `setState({ cheatSheet: null, lastIntake: null, screen: 'intake' })`. Also reset all intake form input values to empty (clear each by id). Consent stays as is.
    - `#backToIntakeFromResultButton`: `setState({ screen: 'intake' })`. Form repopulates from `appState.lastIntake` in the intake branch of render().
    - `#errorTryAgainButton`: re-run the generate flow with the same `appState.lastIntake` and `appState.apiKey`. Wrap in a helper `runGeneration()` (Task 4 defines it) and call that. (You can leave the click handler body as a single call to `runGeneration()`; Task 4 fills in the function.)
    - `#errorBackToIntakeButton`: `setState({ screen: 'intake' })`. Form repopulates.
    - `#errorUpdateKeyButton`: `setState({ screen: 'key-entry' })`. `appState.lastIntake` and `appState.consentGiven` are NOT cleared. The intake form will re-populate when the user saves a new key (DOMContentLoaded path is unchanged; the save-key handler routes to 'intake' from Plan 01).

    Also: ensure the intake render branch (from Plan 01) **repopulates** the form from `appState.lastIntake` whenever the user returns to it. This was set up in Plan 01; verify it actually fires here by adding a small note in code.

    Stop-the-clock rule: every transition into a non-`'generating'` screen must call `stopLoadingMessages()` to clear the interval.

    Hard constraints:
    - Every dynamic DOM write uses `textContent` or `document.createElement` + `appendChild`. Zero `innerHTML` assignments.
    - The raw JSON `<pre>` is `textContent`, not `innerHTML`.
    - Error UI never displays the raw API key.
    - All headings and body copy use only ASCII punctuation; no em or en dashes.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "node -e \"const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const checks={genSection:/<section id=\\\"generating\\\"/.test(s),loadingMsgEl:/id=\\\"loadingMessage\\\"/.test(s),resultSection:/<section id=\\\"result\\\"/.test(s),resultBlockB:/id=\\\"resultBlockB\\\"/.test(s),resultBlockC:/id=\\\"resultBlockC\\\"/.test(s),resultBlockE:/id=\\\"resultBlockE\\\"/.test(s),resultBlockF:/id=\\\"resultBlockF\\\"/.test(s),resultRawJson:/id=\\\"resultRawJson\\\"/.test(s),newCheatBtn:/id=\\\"newCheatSheetButton\\\"/.test(s),backToIntakeRes:/id=\\\"backToIntakeFromResultButton\\\"/.test(s),errorPanel:/<section id=\\\"errorPanel\\\"/.test(s),errHeading:/id=\\\"errorHeading\\\"/.test(s),errBody:/id=\\\"errorBody\\\"/.test(s),errTry:/id=\\\"errorTryAgainButton\\\"/.test(s),errBack:/id=\\\"errorBackToIntakeButton\\\"/.test(s),errUpdate:/id=\\\"errorUpdateKeyButton\\\"/.test(s),errDetailPre:/id=\\\"errorDetailPre\\\"/.test(s),copyAuth:/Your API key did not work/.test(s),copyRate:/rate-limiting your account right now/.test(s),copyServer:/The AI service is having a problem/.test(s),copyParse:/Something went wrong reading the response/.test(s),copyNetwork:/Could not reach the AI service/.test(s),copyAuthBody:/the AI service rejected your saved API key/i.test(s),renderBlockB:/function\\s+renderBlockB\\s*\\(/.test(s),renderBlockC:/function\\s+renderBlockC\\s*\\(/.test(s),renderBlockE:/function\\s+renderBlockE\\s*\\(/.test(s),renderBlockF:/function\\s+renderBlockF\\s*\\(/.test(s),renderGeneratingBranch:/case\\s*'generating'|screen\\s*===\\s*'generating'/.test(s),renderResultBranch:/case\\s*'result'|screen\\s*===\\s*'result'/.test(s),renderErrorBranch:/case\\s*'error'|screen\\s*===\\s*'error'/.test(s),stopLoadingCalled:/stopLoadingMessages\\s*\\(\\)/.test(s),noInnerHtml:!/\\.innerHTML\\s*=/.test(s),noAnthropicInErrorUi:!/(errorHeading|errorBody)[\\s\\S]{0,200}Anthropic/.test(s),noEmDash:!s.includes('—'),noEnDash:!s.includes('–')};let fail=0;for(const k in checks){console.log(k,checks[k]);if(!checks[k])fail++;}process.exit(fail===0?0:1);\""</automated>
    <acceptance_criteria>
      - All 35 checks pass.
      - All five error-class headings/bodies use `the AI service` (or `The AI service`), never `Anthropic` in UI-visible text.
      - render() has a branch for `'generating'`, `'result'`, and `'error'`.
      - No `.innerHTML =` anywhere.
    </acceptance_criteria>
  </verify>
  <done>
    The three new screens are present in the DOM and reachable via setState. Render helpers populate the four result blocks. The error panel switches copy on appState.errorClass. The Update key button only appears on auth errors. All copy matches Section 7 verbatim, with the `Anthropic -> the AI service` substitution applied throughout.
  </done>
</task>

<task type="auto">
  <name>Task 4: Replace the Plan 01 Generate-handler stub with runGeneration(), wire mock + real paths end to end</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/02-llm-round-trip-english/02-RESEARCH.md Section 8 (state machine), Section 10 (mock flag)
    - index.html post-Task-3 - find the Plan 01 stub comment `Plan 02 wires the LLM call here`
  </read_first>
  <action>
    Add a new async helper `runGeneration()` near the other LLM helpers from Task 2. Then update the Generate button click handler to call it. Logic:

    ```
    async function runGeneration() {
      // appState.lastIntake and appState.apiKey must already be set.
      setState({ screen: 'generating', errorClass: null, errorDetail: null });
      const result = await callAnthropic(appState.lastIntake, appState.apiKey);

      // Three outcome shapes:
      //   { ok: true, data: <fixture object> }                    -> mock=1 success
      //   { ok: true, data: null, rawText: '<json string>' }      -> real success OR mock=broken
      //   { ok: false, errorClass, status?, bodyText?, message? } -> failure
      if (!result.ok) {
        setState({
          screen: 'error',
          errorClass: result.errorClass,
          errorDetail: {
            httpStatus: result.status,
            bodyText: (result.bodyText || '').slice(0, 2000),
            message: result.message,
          },
        });
        return;
      }

      // Success branch: either already-parsed (mock=1) or raw text to parse.
      const parsed = parseAndValidateResponse(result.data !== null ? result.data : result.rawText);
      if (!parsed.ok) {
        setState({
          screen: 'error',
          errorClass: parsed.errorClass,
          errorDetail: { message: parsed.message, bodyText: parsed.rawText },
        });
        return;
      }

      setState({ screen: 'result', cheatSheet: parsed.cheatSheet });
    }
    ```

    Then **replace the Plan 01 stub body** in the Generate button click handler. Find the comment marker `// Stub: Phase 2 Plan 02 replaces this body with the LLM round-trip.` (left by Plan 01) and replace the stubbed `console.warn` call with:

    ```
    // Build the intake snapshot and store consent BEFORE firing the call.
    const snapshot = { permit, reason, caseRef, ahv, appointmentDate, situation };
    if (!appState.consentGiven && document.getElementById('consentCheckbox').checked) {
      saveConsent();
      appState.consentGiven = true;
    }
    setState({ lastIntake: snapshot });
    runGeneration();
    ```

    Keep the variable extraction (reading `permit`, `reason`, etc. from the form) that Plan 01 already set up; only the trailing stub line changes. Remove the `console.warn('Plan 02 wires the LLM call here...')` line.

    Hard constraints:
    - The Generate handler must be synchronous (no `async`) so the click event resolves quickly; `runGeneration()` is fired-and-forgotten with no await. This is intentional - the screen has already swapped to 'generating' inside runGeneration.
    - `appState.lastIntake` MUST be set before `runGeneration()` is called (see snapshot block above).
    - On error, `appState.lastIntake` is preserved (we set it before the await, never clear it in error paths).
    - The 'Try again' error button calls `runGeneration()` directly; no extra wrapping needed.

    Also add a small `intakeFromState()` helper that the `'intake'` branch of render() can call to repopulate the form fields from `appState.lastIntake` when non-null. This makes the post-error recovery path observable.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "node -e \"const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const checks={hasRunGeneration:/async\\s+function\\s+runGeneration\\s*\\(/.test(s),runGenSetsGenerating:/setState\\(\\{\\s*screen:\\s*'generating'/.test(s),runGenAwaitsCall:/await\\s+callAnthropic\\s*\\(\\s*appState\\.lastIntake\\s*,\\s*appState\\.apiKey/.test(s),runGenRoutesError:/setState\\(\\{\\s*screen:\\s*'error'/.test(s),runGenRoutesResult:/setState\\(\\{\\s*screen:\\s*'result'/.test(s),stubGone:!/Plan 02 wires the LLM call here/.test(s),generateHandlerCallsRunGen:/runGeneration\\s*\\(\\s*\\)/.test(s),snapshotBeforeAwait:/setState\\(\\{\\s*lastIntake:\\s*snapshot\\s*\\}\\)/.test(s),tryAgainCallsRunGen:/errorTryAgainButton[\\s\\S]{0,400}runGeneration|runGeneration[\\s\\S]{0,400}errorTryAgainButton/.test(s),hasIntakeFromState:/function\\s+intakeFromState\\s*\\(/.test(s),noInnerHtml:!/\\.innerHTML\\s*=/.test(s),noLogApiKey:!/console\\.\\w+\\([^)]*apiKey/.test(s),noEmDash:!s.includes('—'),noEnDash:!s.includes('–')};let fail=0;for(const k in checks){console.log(k,checks[k]);if(!checks[k])fail++;}process.exit(fail===0?0:1);\""</automated>
    <acceptance_criteria>
      - All 14 checks pass.
      - The `Plan 02 wires the LLM call here` stub marker is gone.
      - Generate click results in (a) appState.lastIntake snapshot, (b) screen swap to 'generating', (c) call to runGeneration.
      - Try again calls runGeneration without resetting lastIntake.
    </acceptance_criteria>
  </verify>
  <done>
    Generate flows end to end through runGeneration into either 'result' or 'error'. Try again replays the same intake. The mock=1 URL flag returns the fixture without hitting Anthropic. The mock=broken flag exercises the parse-error branch.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update README.md with the mock flag and cost note</name>
  <files>README.md</files>
  <read_first>
    - README.md (current Phase 1 contents)
    - .planning/phases/02-llm-round-trip-english/02-RESEARCH.md Section 4 (cost), Section 10 (mock flag), Section 11 (out of scope reminders relevant to users)
  </read_first>
  <action>
    Add a new section to README.md titled `Mock fixtures (dev only)` documenting two URL flags:

    - `?mock=1` - returns a hardcoded cheat sheet fixture (MOCK_CHEAT_SHEET) instead of calling the AI service. Useful for iterating on UI without burning API tokens.
    - `?mock=broken` - returns a deliberately malformed JSON string; the app routes to the parse-error screen. Useful for testing the error path.

    Add a one-line cost note in or near the existing `Live URL` section:
    > Each real generation costs roughly USD 0.01 against `claude-haiku-4-5` (about 1500 input tokens and 1200 output tokens, per Anthropic's published pricing as of 2026-05-14).

    Add a one-line model-deprecation reminder:
    > If the AI service returns a 404 from the messages endpoint, the model id may have been retired. Update `ANTHROPIC_API.model` in `index.html` and consult the AI provider's model deprecation page.

    Do NOT change the existing Phase 1 sections (live URL, spike URL, mic guardrail command, known traps). Add the new sections at the bottom or in a sensible place; preserve all previous content.

    No em dashes, no en dashes. Visible copy uses `the AI service` or `the AI provider` (not `Anthropic`); the code-config reference `ANTHROPIC_API.model` is allowed because it names a literal identifier, not a brand-facing string.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "node -e \"const fs=require('fs');const s=fs.readFileSync('README.md','utf8');const checks={mockFlagDocumented:/\\?mock=1/.test(s),mockBrokenDocumented:/\\?mock=broken/.test(s),costNote:/0\\.01/.test(s)&&/claude-haiku-4-5/.test(s),deprecationNote:/404/.test(s)&&/ANTHROPIC_API\\.model/.test(s),noEmDash:!s.includes('—'),noEnDash:!s.includes('–')};let fail=0;for(const k in checks){console.log(k,checks[k]);if(!checks[k])fail++;}process.exit(fail===0?0:1);\""</automated>
    <acceptance_criteria>
      - All 6 checks pass.
      - Both mock flags are mentioned by name.
      - Cost (`0.01`) and model id (`claude-haiku-4-5`) both appear.
      - Deprecation reminder mentions `404` and `ANTHROPIC_API.model`.
      - No em/en dashes anywhere in README.md.
    </acceptance_criteria>
  </verify>
  <done>
    README documents both mock flags, the per-call cost, and the model-deprecation reminder.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Francisco runs one real generation against claude-haiku-4-5</name>
  <what-built>
    Plan 02 implementation is complete: SYSTEM_PROMPT, CHEAT_SHEET_SCHEMA, MOCK_CHEAT_SHEET, ANTHROPIC_API config, callAnthropic / parseAndValidateResponse / runGeneration helpers, generating + result + error screens, ?mock=1 / ?mock=broken flags, README update. All five automated verifies (Tasks 1 through 5) exited 0.
  </what-built>
  <how-to-verify>
    Francisco must run three smoke tests against the live `index.html`. Steps:

    **A. Mock happy path (no API tokens spent).**
    1. Open `file:///.../index.html?mock=1` in desktop Chrome.
    2. Confirm the intake screen loads with the masked key header strip.
    3. Fill all required fields: pick a permit (e.g. B), pick a reason (e.g. renewal), type at least one sentence in the situation textarea, check the consent box (first time).
    4. Click Generate cheat sheet.
    5. Within ~1 second, the screen swaps to a loading view with a cycling reassurance message.
    6. Within ~1 second more, the screen swaps to the result view. Confirm:
       - Block B shows a user goal sentence + gloss + 4 opening lines.
       - Block C shows 4 questions, each with Yes and No answers.
       - Block E shows 6 vocabulary entries with `der`/`die`/`das`.
       - Block F shows 5 checklist items.
       - The collapsible `Raw JSON for debugging` contains valid pretty-printed JSON.
    7. Click `New cheat sheet`. The form is empty. Click `Back to intake` from the result -> the form is repopulated.

    **B. Mock parse-error path (no API tokens spent).**
    1. Open `file:///.../index.html?mock=broken`.
    2. Fill and submit the intake.
    3. The screen swaps to the error panel with heading `Something went wrong reading the response` and the parse-class body. The `Show technical details` collapsible reveals the malformed JSON text.
    4. Click `Try again`. The same error reappears (mock is deterministic). Click `Back to intake`. The form is repopulated.

    **C. Real Anthropic generation (BURNS ~USD 0.01 of API budget).**
    1. Open `file:///.../index.html` (no mock flag) with Francisco's personal Anthropic key saved in localStorage.
    2. Fill the intake with a realistic situation (e.g. B-permit holder calling about renewal, AHV provided, situation: "My current B permit expires in three months and I want to ask what documents I need to bring to my appointment.")
    3. Click Generate. Wait 10 to 20 seconds. Observe the loading messages cycling.
    4. On success, eyeball the four blocks. Francisco confirms:
       - The German in Block B is formal Hochdeutsch, polite, and matches the user goal.
       - Block C questions are plausible questions a Zürich Migrationsamt officer might actually ask for a renewal call.
       - Block E words are relevant (e.g. Bewilligung, Verlängerung, Antrag) with correct articles.
       - Block F items are actionable.
       - No contact details, phone numbers, or addresses are hallucinated anywhere in the output (Block A is hardcoded by Phase 1, not in the LLM output).
       - No legal-eligibility advice ("you qualify for X") appears anywhere.
    5. Optional: try `?mock=1` plus an incorrect path (e.g. clear the saved key, then submit) - this should not require Francisco; the auth-error path is covered by the mock testing in step A above.

    **D. Optional secondary checks (Francisco's discretion):**
    1. Paste a bogus key like `sk-ant-bogus-key-1234` and submit a real intake. Confirm the auth-error screen appears with the `Update key`, `Try again`, `Back to intake` triplet. Click Update key -> save the real key -> land back on intake with the form preserved.
    2. Open DevTools Network panel during the real generation. Confirm the request to `api.anthropic.com/v1/messages` includes `x-api-key`, `anthropic-version: 2023-06-01`, `anthropic-dangerous-direct-browser-access: true`. Confirm the request body contains `output_config` with `format.type: json_schema`. Confirm no other outbound requests (no analytics, no third-party CDN) are issued.

    The critical question Francisco answers: **is the German sensible enough to base Phase 3's rendering work on?** If yes, Plan 02 is approved and Phase 2 closes. If the German is wrong in a structural way (wrong register, wrong vocabulary, wrong question shape for Migrationsamt), Plan 02 needs a system-prompt revision before Phase 3 starts.
  </how-to-verify>
  <resume-signal>
    Type `approved` if all three smoke tests (A, B, C) pass AND the German output from the real call (C) is sensible. Otherwise, describe the specific failure (which screen, what was wrong with the German or the structure) so the executor can patch SYSTEM_PROMPT or the schema before Phase 3.
  </resume-signal>
</task>

</tasks>

<verification>
End-to-end:
- Tasks 1 through 5 each exit 0 on their automated verify.
- `verify-no-mic.ps1` exits 0 (TRUST-04 carry-forward).
- Task 6 returns `approved`.
- A grep for `Anthropic` against user-visible HTML strings (excluding code comments and identifiers) returns zero hits in the rendered UI copy: the error panel, headings, and helper text all say `the AI service` or `the AI provider`.
</verification>

<success_criteria>
- INTAKE-02..05 satisfied via the Plan 01 form (carried forward).
- LLM-01 satisfied: SYSTEM_PROMPT contains explicit `<forbidden_behaviour>` covering contact details, permit-eligibility advice, strategic legal advice.
- LLM-02 satisfied: every successful generation produces a parsed cheatSheet matching CHEAT_SHEET_SCHEMA (verified by parseAndValidateResponse and by Task 6's human check).
- LLM-03 satisfied: generating screen shows cycling reassurance messages during the 10 to 20 second wait.
- LLM-04 satisfied: 401, 429, 4xx-other/5xx, parse-fail, and network-fail all land on the error panel with distinct copy and the documented button set; `Update key` only on auth.
- TRUST-02 satisfied: consent block appears on first generation only and persists across reloads under `migrationsamt.consentGiven`.
- TRUST-04 carry-forward: `verify-no-mic.ps1` still exits 0.
- The result screen renders all four blocks with all locked descriptive field names readable.
- README documents `?mock=1`, `?mock=broken`, the ~USD 0.01 per-call cost, and the 404-on-model-retired remediation step.
- The API key is never logged, never echoed to DOM after save, never serialised into a non-Anthropic-bound payload.
</success_criteria>

<output>
After Task 6 returns `approved`, create `.planning/phases/02-llm-round-trip-english/02-02-SUMMARY.md` per the template at `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Final shapes of ANTHROPIC_API, SYSTEM_PROMPT (note its length and the Phase 5 tuning flag), CHEAT_SHEET_SCHEMA
- The MOCK_CHEAT_SHEET fixture verbatim (so future maintainers can compare against real LLM output)
- The mock flag behaviours (`?mock=1`, `?mock=broken`) and how to verify them locally
- The error-class -> copy table actually shipped (in case wording drifted from this plan)
- Francisco's verdict from Task 6 and any system-prompt or schema deltas applied during the checkpoint
- Any deviations from this plan
- Items flagged for Phase 5 pilot debrief (system prompt tuning, off-topic robustness, etc.)
</output>
