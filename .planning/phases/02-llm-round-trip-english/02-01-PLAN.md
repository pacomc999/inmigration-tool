---
phase: 02-llm-round-trip-english
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - INTAKE-02
  - INTAKE-03
  - INTAKE-04
  - INTAKE-05
  - TRUST-02
tags: [intake, state-machine, consent, trust-02, phase-2]
decisions:
  - "appState.screen union extended to 'key-entry' | 'intake' | 'generating' | 'result' | 'error' (Phase 1's 'ready' is dropped as a reachable state; the key-save handler routes directly to 'intake'). Per 02-RESEARCH.md Section 8."
  - "appState gains four new fields: lastIntake (object or null), cheatSheet (object or null), errorClass (string or null), errorDetail (object or null), consentGiven (boolean). Initial values: nulls and false."
  - "Replace key and Clear key buttons move into a small header strip at the top of the intake screen (text: 'Key: sk-ant-...abcd  [Replace key] [Clear key]'), so the user never lands on a separate 'ready' page. Per 02-RESEARCH.md Section 8."
  - "Permit dropdown has 9 options exactly: L, B, C, Ci, G, N, F, S, unknown (per INTAKE-02 and 02-RESEARCH.md Section 5 labels)."
  - "Reason dropdown has 10 options exactly: renewal, change_status, family_reunification, work_permit_change, address_change, missing_document, lost_permit, status_check, appointment_booking, other (per INTAKE-03 and 02-RESEARCH.md Section 5 labels)."
  - "Three optional reference inputs: caseRef (text), ahv (text, NO format validation - the LLM does not need it), appointmentDate (input type=date). Per INTAKE-04."
  - "Free-text textarea id=situationText, required, no max length. Per INTAKE-05."
  - "Consent checkbox is rendered only when appState.consentGiven is false. Once the user submits with it checked, persist 'true' under localStorage key 'migrationsamt.consentGiven' AND set appState.consentGiven = true so re-renders skip the block. Per TRUST-02 and 02-RESEARCH.md Section 5."
  - "Consent persists for the session AND across reloads (localStorage). TRUST-02 says 'first-time before first generation' - simplest reading is 'once ever, until the user clears all data'."
  - "Generate button is disabled while required fields are empty OR (consent not yet given AND consent checkbox unchecked). Enabling is a single validate() function called on every input event."
  - "Phase 2 ships no Generate-click submission handler yet (that wires the LLM call, which is Plan 02). For Plan 01, the click handler is a stub that snapshots intake into appState.lastIntake, persists consent, and console.warns 'Plan 02 wires the LLM call here'. This keeps Plan 01 demoable on its own without the LLM piece."
  - "Plan 01 does NOT add the generating, result, or error screen DOM. Those are Plan 02. Plan 01 only adds 'intake' to the screen union and the intake screen HTML."
  - "All copy is dash-free (no em or en dashes), per CLAUDE.md."

must_haves:
  truths:
    - "User who has saved a valid-looking key from Phase 1 lands directly on the intake form on reload (no separate 'ready' page)"
    - "User can pick a permit type from a dropdown with exactly 9 options (L, B, C, Ci, G, N, F, S, I don't know)"
    - "User can pick a reason for calling from a dropdown with exactly 10 curated options"
    - "User can fill three optional reference fields (case reference number, AHV number, appointment date) and leave any of them blank"
    - "User can write a free-text situation description in English in a textarea"
    - "User sees a consent block (heading + paragraph + checkbox) on the first visit when consentGiven is false"
    - "User cannot click Generate until required fields are non-empty AND consent checkbox is checked (when shown)"
    - "Replace key and Clear key controls are visible at the top of the intake screen"
    - "After Clear key + confirm, the app removes both localStorage entries (key and consent) and routes to 'key-entry'"
    - "appState.lastIntake is populated when the user clicks Generate (Plan 02 will consume this)"
  artifacts:
    - path: "index.html"
      provides: "Intake screen HTML/CSS/JS, appState extension, key header strip, consent block, validation"
      contains: "id=\"intake\""
    - path: "index.html"
      provides: "appState fields lastIntake, cheatSheet, errorClass, errorDetail, consentGiven"
      contains: "consentGiven"
  key_links:
    - from: "Save key button handler"
      to: "intake screen"
      via: "setState({ apiKey: trimmed, screen: 'intake' }) replaces the previous setState({...screen: 'ready'})"
      pattern: "setState\\(\\{[^}]*screen:\\s*'intake'"
    - from: "Generate button click"
      to: "appState.lastIntake snapshot"
      via: "handler reads form fields, builds intake object, calls setState({ lastIntake: ..., consentGiven: true if checkbox just checked })"
      pattern: "lastIntake"
    - from: "DOMContentLoaded"
      to: "appState.consentGiven mirror from localStorage"
      via: "localStorage.getItem('migrationsamt.consentGiven') === 'true' on load"
      pattern: "migrationsamt\\.consentGiven"
---

<objective>
Extend the Phase 1 skeleton with the structured intake form, the consent checkbox, the screen union update (drop 'ready', add 'intake'), and the inline key-management header strip. After Plan 01, a user with a saved key lands on the intake screen, can fill out a valid intake, and clicking Generate snapshots the intake into appState.lastIntake (the LLM call itself is Plan 02).

Purpose: Build the half of Phase 2 that does not require a live Anthropic call. Get the form, validation, consent gate, and state machine in shape before wiring the LLM. This keeps the LLM work in Plan 02 small and focused.

Output: Modified `index.html` containing the new intake section, the key header strip on the intake screen, the extended appState shape, and the consent persistence logic. No new files. No README change yet (Plan 02 owns the README update).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-llm-round-trip-english/02-CONTEXT.md
@.planning/phases/02-llm-round-trip-english/02-RESEARCH.md
@.planning/phases/01-skeleton-state-key-constants/01-01-SUMMARY.md
@.planning/phases/01-skeleton-state-key-constants/01-02-SUMMARY.md
@index.html
@CLAUDE.md

<interfaces>
<!-- Phase 1 shipped these identifiers in index.html. Do NOT rename them. Extend in place. -->
<!-- appState (current): { screen: 'key-entry' | 'ready', apiKey: string | null } -->
<!-- After this plan: { screen: 'key-entry' | 'intake' | 'generating' | 'result' | 'error',
                         apiKey, lastIntake, cheatSheet, errorClass, errorDetail, consentGiven } -->
<!-- STORAGE_KEY = 'migrationsamt.anthropicKey' (Phase 1, do not touch) -->
<!-- New localStorage key introduced by this plan: 'migrationsamt.consentGiven' (string 'true' or absent) -->
<!-- maskKey(key) -> string (Phase 1; reuse for the header strip) -->
<!-- setState(partial), render() (Phase 1; extend render switch with 'intake' branch) -->
<!-- Phase 1's #keyEntry and #ready sections exist. Keep #keyEntry, hide #ready (or delete it).
     Recommended: keep the #ready DOM node but never route to 'ready' in render(); drop the
     'ready' branch from the screen union and from the render() switch. -->
<!-- INTAKE-02 permit values (from 02-RESEARCH.md Section 5):
       L, B, C, Ci, G, N, F, S, unknown (placeholder option has empty value).
     INTAKE-03 reason values:
       renewal, change_status, family_reunification, work_permit_change,
       address_change, missing_document, lost_permit, status_check,
       appointment_booking, other (placeholder option has empty value).
     Display labels per 02-RESEARCH.md Section 5 verbatim. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend appState, render(), and key handlers for the new screen union</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/02-llm-round-trip-english/02-RESEARCH.md Section 8 (state machine)
    - index.html lines 270 to 410 (current appState, setState, render, key handlers, DOMContentLoaded)
  </read_first>
  <action>
    Update the inline `<script>` block in index.html as follows. Make a single coherent edit; do not split into multiple commits.

    1. **Extend `appState`** to add five new fields with their initial values:
       - `lastIntake: null`
       - `cheatSheet: null`
       - `errorClass: null`
       - `errorDetail: null`
       - `consentGiven: false`
       Keep the existing `screen` and `apiKey` fields. Update the screen comment to list the new union: `'key-entry' | 'intake' | 'generating' | 'result' | 'error'`. Add a short banner comment explaining that Phase 2 widens the state machine (per 02-RESEARCH.md Section 8).

    2. **Add a consent storage helper** alongside the existing STORAGE_KEY block:
       - `const CONSENT_STORAGE_KEY = 'migrationsamt.consentGiven';`
       - Two small helpers: `loadConsent()` returns boolean (try/catch around `localStorage.getItem`; treat any throw as false), and `saveConsent()` writes the literal string `'true'` (try/catch, best effort, silent on failure - the consent gate is not security critical).

    3. **Rewrite `render()`** to switch on five screen values. For Plan 01, only the `'key-entry'` and `'intake'` branches do work; `'generating'`, `'result'`, and `'error'` branches should call a placeholder helper `renderUnimplemented(screen)` that hides every screen section and writes a one-line console.warn so the executor of Plan 02 can find it. The `'ready'` branch is removed from the switch. If `appState.screen === 'ready'` is ever observed (defensive), coerce to `'intake'` via `setState({ screen: 'intake' })` and return.

       The `'key-entry'` branch is unchanged from Phase 1.

       The `'intake'` branch must:
       - Hide `#keyEntry` and `#ready` (if `#ready` still exists in the DOM).
       - Show the new `#intake` section (hidden attribute off).
       - Populate the masked-key header strip (`#intakeMaskedKey.textContent = maskKey(appState.apiKey)`).
       - Toggle the consent block visibility based on `appState.consentGiven`: if true, set the `#intakeConsent` section's `hidden` attribute; if false, clear it.
       - Repopulate form fields from `appState.lastIntake` if it is non-null (so the user comes back to a populated form after Plan 02's error path). For Plan 01 this branch is reachable but `lastIntake` will be null on first arrival; the repopulation code must not crash when `lastIntake` is null.
       - Call `validateIntake()` (defined in Task 2) to set the Generate button's `disabled` state.

    4. **Update the Save key handler** so a successful save calls `setState({ apiKey: trimmed, screen: 'intake' })` instead of `screen: 'ready'`. Also clear the password input field (already done in Phase 1; keep it).

    5. **Update the Clear key handler** to ALSO remove the consent flag (`localStorage.removeItem(CONSENT_STORAGE_KEY)` inside try/catch) and reset `appState.consentGiven` to false. Then `setState({ apiKey: null, lastIntake: null, consentGiven: false, screen: 'key-entry' })`. The window.confirm prompt is unchanged.

    6. **Update the Replace key handler** to `setState({ screen: 'key-entry' })` only (no key removal, no consent change). Unchanged from Phase 1 except that it now departs from 'intake' rather than 'ready'.

    7. **Update the DOMContentLoaded handler** so that:
       - On load, read the stored key with the existing try/catch.
       - Also read `loadConsent()` and stash on `appState.consentGiven`.
       - If a key exists, set `appState.screen = 'intake'` (NOT 'ready') and `appState.apiKey = stored`.
       - Then call `render()`.

    8. Keep all DOM writes via `textContent`. Do NOT introduce any `innerHTML` assignments anywhere. Do NOT add any `console.log` of `appState.apiKey`. Do NOT add `getUserMedia`. Re-verify mentally before committing.

    9. Add a banner comment block above the new screen-machine code (70-dash banner, matching Phase 1 style) reading something like `STATE MACHINE EXTENSION (Phase 2 Plan 01)` with two or three lines explaining the new screens.

    Keep functions short. Use plain language in comments (Francisco is a beginner).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "node -e \"const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const checks={hasIntakeScreen:/screen:\s*'intake'/.test(s),hasConsentField:/consentGiven:\s*false/.test(s),hasLastIntakeField:/lastIntake:\s*null/.test(s),hasCheatSheetField:/cheatSheet:\s*null/.test(s),hasErrorClassField:/errorClass:\s*null/.test(s),hasErrorDetailField:/errorDetail:\s*null/.test(s),hasConsentStorageKey:/CONSENT_STORAGE_KEY\s*=\s*'migrationsamt\.consentGiven'/.test(s),hasLoadConsent:/function\s+loadConsent\s*\(/.test(s),hasSaveConsent:/function\s+saveConsent\s*\(/.test(s),saveKeyRoutesToIntake:/setState\(\{[^}]*apiKey[^}]*screen:\s*'intake'/.test(s),clearKeyRemovesConsent:/removeItem\(CONSENT_STORAGE_KEY\)/.test(s),domLoadedRoutesToIntake:/appState\.screen\s*=\s*'intake'/.test(s),noReadyInSwitch:!/case\s*'ready'/.test(s)&&!(/screen\s*===\s*'ready'/.test(s)&&!/coerce/i.test(s)),noInnerHtmlAssignment:!/\.innerHTML\s*=/.test(s),noConsoleLogKey:!/console\.log\([^)]*apiKey/.test(s),noGetUserMedia:!/getUserMedia/.test(s)};let fail=0;for(const k in checks){console.log(k,checks[k]);if(!checks[k])fail++;}process.exit(fail===0?0:1);\""</automated>
    <acceptance_criteria>
      - All 16 named checks print `true` and node exits 0.
      - No occurrence of `case 'ready'` or active `screen === 'ready'` routing (defensive coercion is allowed if a comment marks it).
      - No `.innerHTML =` anywhere in index.html.
      - No `console.log(...apiKey...)` anywhere.
      - No `getUserMedia` anywhere.
    </acceptance_criteria>
  </verify>
  <done>
    appState carries the five new fields; render() switches on the new five-value union; Save key routes to 'intake' and not 'ready'; Clear key drops the consent flag too; DOMContentLoaded routes a saved-key user directly to 'intake'; all changes added with banner comments and beginner-friendly inline comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the intake screen HTML, the key header strip, and the consent block</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/02-llm-round-trip-english/02-RESEARCH.md Section 5 (intake form UI shape and copy verbatim)
    - index.html lines 100 to 270 (current `<main>` containing #keyEntry and #ready)
  </read_first>
  <action>
    Add a new `<section id="intake" hidden>` inside `<main>` in index.html, immediately after the existing `#ready` section. Keep the page layout and CSS conventions from Phase 1. All copy strings are taken verbatim from 02-RESEARCH.md Section 5; do not paraphrase.

    Order of elements inside `#intake`, top to bottom:

    1. **Key header strip** (small, single-line). A `<div class="intake-key-header">` containing:
       - The literal text `Key:` followed by a `<span id="intakeMaskedKey">` (populated by render()).
       - Two `<button type="button">` controls: `id="intakeReplaceKey"` with label `Replace key`, and `id="intakeClearKey"` with label `Clear key`. Both wire to the existing Replace/Clear handlers from Phase 1 (the handlers do not need to know which button triggered them; bind both buttons to the same handler functions).

    2. **H2 heading**: `Tell us about your call`.

    3. **Helper paragraph** (class `helper`): `Fill in what you know. Optional fields can be skipped. The cheat sheet is built from this in one single call to the AI service.`

    4. **INTAKE-02 - Permit dropdown**.
       - Label: `What permit do you hold?`
       - `<select id="permitType" required>` with these 10 options in this exact order (placeholder first):
         - value="" (selected, disabled) label `Choose one`
         - value="L" label `L permit (short-term residence)`
         - value="B" label `B permit (residence)`
         - value="C" label `C permit (settlement)`
         - value="Ci" label `Ci permit (residence with gainful employment for spouses of diplomats)`
         - value="G" label `G permit (cross-border commuter)`
         - value="N" label `N permit (asylum seeker)`
         - value="F" label `F permit (temporary admission)`
         - value="S" label `S permit (protection status)`
         - value="unknown" label `I don't know`

    5. **INTAKE-03 - Reason dropdown**.
       - Label: `What is the call about?`
       - `<select id="callReason" required>` with these 11 options (placeholder first):
         - value="" (selected, disabled) label `Choose one`
         - value="renewal" label `Permit renewal`
         - value="change_status" label `Change of status`
         - value="family_reunification" label `Family reunification`
         - value="work_permit_change" label `Work-permit change`
         - value="address_change" label `Address change`
         - value="missing_document" label `Missing document`
         - value="lost_permit" label `Lost permit`
         - value="status_check" label `Status check on a pending application`
         - value="appointment_booking" label `Booking an appointment`
         - value="other" label `Other`

    6. **INTAKE-04 - Reference details (optional)**. Subheading: `Reference details (optional)`. Helper: `Skip any you do not have. The cheat sheet still works without them.`
       - Label `Case reference number` over `<input type="text" id="caseRef" placeholder="e.g. ZH-12345">`
       - Label `AHV number` over `<input type="text" id="ahv" placeholder="756.xxxx.xxxx.xx">` (NO format validation - do not add pattern attribute)
       - Label `Appointment date, if any` over `<input type="date" id="appointmentDate">`

    7. **INTAKE-05 - Free-text situation** (required).
       - Label: `Briefly describe your situation in English`
       - `<textarea id="situationText" rows="4" required></textarea>`
       - Helper below: `A few sentences is enough. The tool uses this to tailor the questions and checklist. Do not paste anything you would not want sent to the AI service.`

    8. **Consent block** (`<section id="intakeConsent">`, initially `hidden` toggled by render()).
       - Heading: `Before we send this to the AI service`
       - Paragraph copy verbatim from 02-RESEARCH.md Section 5 consent block, with one wording adjustment for generic tone:
         > When you click "Generate cheat sheet", the following information is sent in one single API call to the AI service using your saved API key:
         > - your permit type and the reason for your call
         > - any reference numbers, AHV number, and appointment date you typed in
         > - the free-text description of your situation you just wrote
         >
         > Nothing else leaves your browser. There is no backend, no logs, no analytics. The AI provider's own data-handling policies apply to the request itself.
         Render the bullet list as a real `<ul>` of three `<li>` elements.
       - `<label><input type="checkbox" id="consentCheckbox"> I understand what is being sent and I want to continue.</label>`

    9. **Generate button**:
       - `<button type="button" id="generateButton" disabled>Generate cheat sheet</button>`

    10. **Inline error region** (used by Plan 01 only for validation hints, not for LLM errors): `<p id="intakeError" class="error" role="status"></p>` (initially empty).

    Add minimal CSS rules to the existing `<style>` block for any new classes you introduce (e.g. `.intake-key-header { display: flex; gap: 0.5rem; align-items: center; font-size: 0.9rem; }`). Match Phase 1's spacing and color conventions. Do not introduce any new external resources.

    Then add a `validateIntake()` function near the existing handlers:
    - Reads `#permitType`, `#callReason`, `#situationText` values.
    - Reads `#consentCheckbox.checked` only if `appState.consentGiven === false` (otherwise consent is satisfied implicitly).
    - Computes a boolean `isValid` = (permit non-empty) AND (reason non-empty) AND (situationText trimmed non-empty) AND (consentGiven OR consentCheckbox.checked).
    - Sets `document.getElementById('generateButton').disabled = !isValid`.
    - Clears `#intakeError.textContent` whenever a previously-invalid form becomes valid; otherwise leaves it as-is (a future plan may set helpful messages there).

    Wire `validateIntake` to the `input` event of all four required inputs (`permitType`, `callReason`, `situationText`, `consentCheckbox`). Call it once from inside the `'intake'` branch of `render()` so the button starts in the right state when the screen appears.

    Wire `#intakeReplaceKey` and `#intakeClearKey` to the same `replaceKey()` and `clearKey()` handlers Phase 1 already exposes. If those handlers are defined as inline arrow functions on the existing buttons, refactor them into named functions (`handleReplaceKey`, `handleClearKey`) so both sets of buttons can call the same logic.

    Finally, add a click handler on `#generateButton`:
    - Build the intake snapshot object: `{ permit, reason, caseRef, ahv, appointmentDate, situation }` reading the trimmed values from each field. Empty strings are allowed for the three optional ref fields.
    - If consent was not previously given AND the checkbox is checked, call `saveConsent()` and set `appState.consentGiven = true`.
    - Call `setState({ lastIntake: snapshot })` (do NOT change screen yet).
    - `console.warn('Plan 02 wires the LLM call here. Intake snapshot:', appState.lastIntake);`
      This stub is intentional - Plan 02 replaces this body with the real LLM call. Add an inline comment marking it: `// Stub: Phase 2 Plan 02 replaces this body with the LLM round-trip.`

    Every DOM write in the intake screen uses `textContent` (the HTML literals you author are fine; only dynamic writes need to be `textContent`). No `innerHTML` assignments. Do not log `appState.apiKey`. Do not introduce `getUserMedia`.

    Banner comment above the new intake section in HTML: `<!-- Phase 2 Plan 01: intake screen (INTAKE-02..05, TRUST-02) -->`. Banner comment above the new JS block in `<script>`: `INTAKE FORM (Phase 2 Plan 01)` with a 70-dash banner matching Phase 1 style.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "node -e \"const fs=require('fs');const s=fs.readFileSync('index.html','utf8');function has(re){return re.test(s);}const checks={sectionIntake:has(/<section id=\\\"intake\\\"/),keyHeaderStrip:has(/id=\\\"intakeMaskedKey\\\"/),intakeReplaceBtn:has(/id=\\\"intakeReplaceKey\\\"/),intakeClearBtn:has(/id=\\\"intakeClearKey\\\"/),permitSelect:has(/id=\\\"permitType\\\"/),permitL:has(/value=\\\"L\\\"/),permitB:has(/value=\\\"B\\\"/),permitC:has(/value=\\\"C\\\"/),permitCi:has(/value=\\\"Ci\\\"/),permitG:has(/value=\\\"G\\\"/),permitN:has(/value=\\\"N\\\"/),permitF:has(/value=\\\"F\\\"/),permitS:has(/value=\\\"S\\\"/),permitUnknown:has(/value=\\\"unknown\\\"/),reasonSelect:has(/id=\\\"callReason\\\"/),reasonRenewal:has(/value=\\\"renewal\\\"/),reasonChangeStatus:has(/value=\\\"change_status\\\"/),reasonFamily:has(/value=\\\"family_reunification\\\"/),reasonWork:has(/value=\\\"work_permit_change\\\"/),reasonAddress:has(/value=\\\"address_change\\\"/),reasonMissing:has(/value=\\\"missing_document\\\"/),reasonLost:has(/value=\\\"lost_permit\\\"/),reasonStatus:has(/value=\\\"status_check\\\"/),reasonAppointment:has(/value=\\\"appointment_booking\\\"/),reasonOther:has(/value=\\\"other\\\"/),caseRefInput:has(/id=\\\"caseRef\\\"/),ahvInput:has(/id=\\\"ahv\\\"/),apptDateInput:has(/id=\\\"appointmentDate\\\"/),textarea:has(/id=\\\"situationText\\\"/),consentSection:has(/id=\\\"intakeConsent\\\"/),consentCheckbox:has(/id=\\\"consentCheckbox\\\"/),generateBtn:has(/id=\\\"generateButton\\\"[^>]*disabled/),validateFn:has(/function\s+validateIntake\s*\(/),genericServiceCopy:has(/the AI service/),noAnthropicInUiCopy:!has(/Anthropic[^a-z]/i)||!has(/<p[^>]*>[^<]*Anthropic/),noEmDash:!s.includes('—'),noEnDash:!s.includes('–'),noInnerHtml:!has(/\.innerHTML\s*=/),noGetUserMedia:!has(/getUserMedia/),noConsoleLogApiKey:!has(/console\.log\([^)]*apiKey/),stubMarker:has(/Plan 02 wires the LLM call here/)};let fail=0;for(const k in checks){console.log(k,checks[k]);if(!checks[k])fail++;}process.exit(fail===0?0:1);\""</automated>
    <acceptance_criteria>
      - All 40 checks return true; node exits 0.
      - User-visible copy does not contain `Anthropic` as a standalone visible string (use `the AI service`). Code comments and devtools strings may still mention Anthropic.
      - No em dash (U+2014) or en dash (U+2013) anywhere in the file.
      - The stub marker comment `Plan 02 wires the LLM call here` is present.
    </acceptance_criteria>
  </verify>
  <done>
    The intake screen is fully present in the DOM (hidden until render() shows it). The Generate button starts disabled and enables only when all required fields are filled and consent is satisfied. Clicking Generate snapshots the intake into appState.lastIntake and console.warns the stub message. Replace key and Clear key controls appear in the intake header. The consent block hides automatically once the user has previously consented.
  </done>
</task>

<task type="auto">
  <name>Task 3: TRUST-04 grep guard + end-of-plan smoke check</name>
  <files>verify-no-mic.ps1</files>
  <read_first>
    - verify-no-mic.ps1 (Phase 1)
    - 02-RESEARCH.md Sections "Security Domain" and "Out of Scope Reminders" (no microphone reminder)
  </read_first>
  <action>
    Run the Phase 1 mic guardrail script as a verification gate. No file modifications are intended in this task; if `verify-no-mic.ps1` no longer covers `index.html` for any reason, fix the script's `-Path` list to include it (it should already - this is a defensive check). Do NOT change the script's logic; the only allowed edit is adding a path or extending the file-extension glob.

    Also confirm that the new intake screen does not introduce any string that would trip a future `innerHTML` review: rerun the Task 1+2 verify regex over the file and confirm zero matches for `.innerHTML =`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "& { $r = & powershell -NoProfile -File ./verify-no-mic.ps1; $code = $LASTEXITCODE; Write-Output $r; if ($code -ne 0) { exit 1 } ; $s = Get-Content index.html -Raw; if ($s -match '\\.innerHTML\\s*=') { Write-Output 'FAIL: innerHTML assignment found'; exit 1 } else { Write-Output 'OK: no innerHTML assignment'; exit 0 } }"</automated>
    <acceptance_criteria>
      - `verify-no-mic.ps1` exits 0 with the line `verify-no-mic: no matches. OK.` in its output.
      - The follow-up `innerHTML` check prints `OK: no innerHTML assignment` and exits 0.
    </acceptance_criteria>
  </verify>
  <done>
    TRUST-04 lifetime guardrail still passes after the Phase 2 Plan 01 edits, and the no-innerHTML rule is unbroken.
  </done>
</task>

</tasks>

<verification>
End-to-end smoke (manual, Francisco runs in desktop Chrome):

1. Open `index.html` with a saved Phase 1 key in localStorage. Confirm the page lands on the intake screen, not on a `Ready` page. The masked key appears in the header strip (`sk-ant-...abcd`) with `Replace key` and `Clear key` next to it.
2. Confirm the permit dropdown shows 10 options (placeholder + 9 real). Confirm the reason dropdown shows 11 options (placeholder + 10 real).
3. Leave the situation textarea empty. Confirm Generate stays disabled.
4. Fill all required fields and the consent checkbox. Confirm Generate enables. Click Generate. The DevTools console shows the `Plan 02 wires the LLM call here. Intake snapshot: { ... }` warning with the captured fields.
5. Reload the page. The consent block is gone (consent persisted). Generate enables as soon as required fields are filled.
6. Click `Clear key`, confirm. Both `migrationsamt.anthropicKey` and `migrationsamt.consentGiven` disappear from DevTools Application Local Storage. The page swaps to `key-entry`.
7. Click `Replace key`. The page swaps to `key-entry` without removing localStorage. Save the same key. Land on `intake` again.
</verification>

<success_criteria>
- All three task automated verifies exit 0.
- INTAKE-02..05 and TRUST-02 are observable in the DOM and behaviour as described.
- `'ready'` is no longer a reachable state in normal flow.
- `verify-no-mic.ps1` continues to exit 0.
- No em or en dashes in the file. No `innerHTML` assignments. No `console.log` of `appState.apiKey`. No `getUserMedia`.
- User-visible copy says `the AI service`, not `Anthropic`. Code comments / console messages may still name Anthropic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-llm-round-trip-english/02-01-SUMMARY.md` following the template at `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Final `appState` shape after this plan
- The new localStorage key (`migrationsamt.consentGiven`) and how Clear key handles it
- The intake form fields and their ids (Plan 02 will reference these by id)
- The stub marker comment in the Generate handler and where Plan 02 should replace it
- Any deviations from this plan
</output>
