---
phase: 01-skeleton-state-key-constants
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - index.html
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TRUST-04
decisions:
  - "D2-01 (constants location): The MIGRATIONSAMT constants object and the BLOCK_D and BLOCK_H constants are added inline at the top of the same <script> block in index.html, above the appState declaration. Same single-file pattern (D1-02 from Plan 01). No separate constants.js per researcher Section 6 recommendation."
  - "D2-02 (constants shape): Use the exact MIGRATIONSAMT object structure from researcher Section 4 verbatim (nameDe, nameEn, parentDept, address, phone, email, website, phoneHours, counterHours, closures2026). Add a '// Sources verified 2026-05-14, https://www.zh.ch/de/sicherheitsdirektion/migrationsamt.html' comment above the object."
  - "D2-03 (Block D panic phrases): use the seven phrases from researcher Section 4 as a const BLOCK_D = [{ de, en }, ...]. Plus a separate const HOCHDEUTSCH_REQUEST = { de, en } since it lives in Block B per Phase 3 but is hardcoded here per ROADMAP Cheat Sheet Anatomy."
  - "D2-04 (Block H footer): const BLOCK_H_FOOTER = { disclaimer, escalations: [{ name, url, phone? }, ...] }. Three escalations: Welcome Desk (Stadt Zürich), MIRSAH (SAH Zürich, with phone +41 44 291 00 15), Solinetz Zürich. URLs verbatim from researcher Section 4."
  - "D2-05 (no rendering in Phase 1): the constants are declared but not rendered in any UI screen yet. Rendering arrives in Phase 3. Phase 1 is content placement only. We must NOT add the constants to appState; they are module-level data, not state."
  - "D2-06 (assumptions to flag for Francisco): A1 (Block D German phrasing) and A2 (24 Dec 2026 Migrationsamt schedule) from researcher Assumptions Log are recorded as code comments next to the relevant constants so Phase 3 cannot miss them."
  - "D2-07 (REQUIREMENTS.md drops KEY-04): KEY-04 (provider toggle Gemini/OpenAI) is obsolete per L-01 / D-01. Remove the bullet under '### API Key & Settings (KEY)'. Remove the KEY-04 row from the Traceability table. Update the 'Every active v1 requirement maps to exactly one phase. 30 of 30 mapped, 0 orphans.' line to 29 of 29."
  - "D2-08 (REQUIREMENTS.md amends SPIKE-01): rewrite SPIKE-01 wording to drop the Gemini/OpenAI/iOS Safari references (superseded by D-01, D-02, D-05). New wording: 'Confirmed via deployed-origin spike that the Anthropic Messages API accepts direct browser fetch calls from the GitHub Pages production host on desktop Chrome, with the anthropic-dangerous-direct-browser-access: true header.' Status remains [x] since Phase 0 passed."
  - "D2-09 (ROADMAP.md updates): in the Phase 1 Requirements line, remove KEY-04 (leaves KEY-01, KEY-02, KEY-03, TRUST-04). In the Phase 1 Success Criteria, replace criterion 2 (Gemini/OpenAI toggle) with: 'Provider is hardcoded to Anthropic per Phase 0 outcome (D-01); the UI states clearly that the saved key is an Anthropic API key, so the user knows what their key is for.' Update the Phase 0 description and Notes block where they still reference Gemini/OpenAI/iOS Safari to mark them superseded. In the Coverage table, remove KEY-04 from the Phase 1 row. Update the Phase 1 'Plans' line from 'TBD' to '2 plans' with bullets pointing at 01-01-PLAN.md and 01-02-PLAN.md."
  - "D2-10 (grep verification scope): the final TRUST-04 grep is run across index.html, the spike/ folder, and the new constants. verify-no-mic.ps1 from Plan 01 already covers index.html and spike/index.html; this plan does not change the script. The verification task here re-runs the script and additionally runs a literal 'git grep' equivalent via PowerShell across the whole repo as a belt-and-suspenders check."
must_haves:
  truths:
    - "index.html contains a top-of-script MIGRATIONSAMT const object with the exact fields nameDe, nameEn, parentDept, address (line1, line2, postalCode, city, country), phone (international, national), email, website, phoneHours (mondayToFriday with two windows; saturday: null; sunday: null), counterHours, and closures2026 (a 2026 array with at least Neujahrstag, Karfreitag, Ostermontag, Tag der Arbeit, Auffahrt, Pfingstmontag, Weihnachtstag)."
    - "index.html contains a const BLOCK_D array with exactly seven phrase objects {de, en} covering: repeat, slower, send by email, I don't understand, I understood, thank you, Auf Wiederhören."
    - "index.html contains a const HOCHDEUTSCH_REQUEST object with German and English text for the 'speak Standard German' request line."
    - "index.html contains a const BLOCK_H_FOOTER object with a disclaimer string and an escalations array of three entries (Welcome Desk, MIRSAH with phone, Solinetz) including the verified URLs."
    - "The Migrationsamt phone number stored in the constant is the verified +41 43 259 88 00 in international form."
    - "The Migrationsamt address stored in the constant is Berninastrasse 45, Postfach, 8090 Zürich."
    - "None of these constants are rendered in any UI section in Phase 1; they are declarations only. The visible UI (key-entry and ready screens from Plan 01) is unchanged."
    - "Across the whole repo, the literal string getUserMedia appears zero times: verify-no-mic.ps1 from Plan 01 exits 0, and a separate full-repo PowerShell Select-String over **/*.html, **/*.js, **/*.ps1 returns no matches."
    - "REQUIREMENTS.md no longer contains a KEY-04 bullet under '### API Key & Settings (KEY)' or in the Traceability table; the orphan-count statement is updated to 29 of 29."
    - "REQUIREMENTS.md SPIKE-01 wording no longer references Gemini, OpenAI, or iOS Safari; new wording references Anthropic, desktop Chrome, and the dangerous-direct-browser-access header."
    - "ROADMAP.md Phase 1 Requirements list no longer contains KEY-04. Phase 1 Success Criterion 2 is rewritten to reflect Anthropic-only (per D2-09). Phase 1 Plans line is updated to list 01-01-PLAN.md and 01-02-PLAN.md."
    - "ROADMAP.md Coverage table Phase 1 row no longer contains KEY-04."
  artifacts:
    - path: "index.html"
      provides: "Hardcoded Migrationsamt constants (Block A facts), Block D panic phrases, Hochdeutsch request line, Block H footer + escalations. Declared as top-of-script const objects."
      contains: "MIGRATIONSAMT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "KEY-04 removed (obsolete per L-01); SPIKE-01 wording amended to Anthropic-only desktop-Chrome-only; orphan-count updated."
      contains: "29 of 29"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 requirements list, success criteria, and plans list updated; Coverage table Phase 1 row aligned."
      contains: "01-01-PLAN.md"
  key_links:
    - from: "index.html MIGRATIONSAMT constant"
      to: "official Migrationsamt facts from zh.ch (Berninastrasse 45, +41 43 259 88 00, phone hours Mon-Fri)"
      via: "static const object at top of <script>, sourced/verified 2026-05-14 per researcher Section 4"
      pattern: "MIGRATIONSAMT\\s*=\\s*\\{"
    - from: "REQUIREMENTS.md Phase 1 list"
      to: "ROADMAP.md Phase 1 row + Coverage table"
      via: "two files updated together so the requirement-to-phase mapping stays consistent (KEY-04 removed from both)"
      pattern: "(?s)Phase 1.*KEY-01.*KEY-02.*KEY-03.*TRUST-04"
    - from: "BLOCK_D, HOCHDEUTSCH_REQUEST, BLOCK_H_FOOTER constants in index.html"
      to: "Phase 3 rendering tasks (CHEAT-02 Block B Hochdeutsch line, CHEAT-04 Block D panic phrases, CHEAT-08 Block H footer + escalations)"
      via: "constants placed in Phase 1 per ROADMAP 'Notes on Phase Boundaries' so Phase 3 has nothing to invent"
      pattern: "BLOCK_D|BLOCK_H_FOOTER|HOCHDEUTSCH_REQUEST"
---

<objective>
Add the hardcoded Migrationsamt facts (Block A contact + 2026 closures), Block D panic phrases, the Hochdeutsch request line, and the Block H footer + escalation links as top-of-script const objects inside index.html. Then run the final TRUST-04 enforcement check (full-repo getUserMedia grep) and update REQUIREMENTS.md + ROADMAP.md to drop the obsolete KEY-04 and amend SPIKE-01's stale wording. No code from these constants is rendered yet; rendering is Phase 3. This plan is content placement + documentation housekeeping.

Purpose: Lock in the load-bearing facts the LLM is permanently forbidden from generating (phone, address, hours, holidays, panic phrases, escalation contacts) so Phase 3 has nothing to invent. Close out TRUST-04 with a final whole-repo grep. Bring REQUIREMENTS.md and ROADMAP.md into sync with the Phase 0 outcome.

Output: index.html updated (constants added, no UI change); REQUIREMENTS.md and ROADMAP.md amended; verify-no-mic.ps1 still passes; new full-repo grep returns zero matches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-state-key-constants/01-CONTEXT.md
@.planning/phases/01-skeleton-state-key-constants/01-RESEARCH.md
@.planning/phases/01-skeleton-state-key-constants/01-01-PLAN.md
@CLAUDE.md

<constants_verbatim>
<!-- Use these exact values. Verified by researcher 2026-05-14 against zh.ch. -->

MIGRATIONSAMT shape (D2-02): see researcher Section 4. Verbatim fields:
  nameDe: 'Migrationsamt des Kantons Zürich'
  nameEn: 'Migration Office of Canton Zurich'
  parentDept: 'Sicherheitsdirektion Kanton Zürich'
  address: { line1: 'Berninastrasse 45', line2: 'Postfach', postalCode: '8090', city: 'Zürich', country: 'Switzerland' }
  phone: { international: '+41 43 259 88 00', national: '043 259 88 00' }
  email: 'info@ma.zh.ch'
  website: 'https://www.zh.ch/de/sicherheitsdirektion/migrationsamt.html'
  phoneHours.mondayToFriday: [{ from: '08:00', to: '11:45' }, { from: '13:00', to: '16:30' }]
  phoneHours.saturday: null
  phoneHours.sunday: null
  counterHours.mondayToFriday: [{ from: '08:00', to: '16:30' }]
  closures2026: 11 entries from researcher Section 4 (Neujahrstag, Berchtoldstag, Karfreitag, Ostermontag, Tag der Arbeit, Tag vor Auffahrt (reduced, closesAt '14:30'), Auffahrt, Pfingstmontag, Bundesfeier, Weihnachtstag, Stephanstag)

BLOCK_D shape (D2-03): an array of seven { de, en } objects. Exact German phrasings per researcher Section 4.
  1. { de: 'Können Sie das bitte wiederholen?', en: 'Could you please repeat that?' }
  2. { de: 'Können Sie bitte langsamer sprechen?', en: 'Could you please speak more slowly?' }
  3. { de: 'Können Sie mir das bitte per E-Mail schicken?', en: 'Could you please send me that by email?' }
  4. { de: 'Ich verstehe nicht.', en: 'I do not understand.' }
  5. { de: 'Ich habe verstanden.', en: 'I understood.' }
  6. { de: 'Vielen Dank.', en: 'Thank you very much.' }
  7. { de: 'Auf Wiederhören.', en: 'Goodbye (phone-specific).' }

HOCHDEUTSCH_REQUEST shape (D2-03):
  { de: 'Können wir bitte auf Hochdeutsch sprechen? Mein Schweizerdeutsch ist nicht so gut.',
    en: 'Could we please speak Standard German? My Swiss German is not very good.' }

BLOCK_H_FOOTER shape (D2-04):
  {
    disclaimer: 'This is a preparation aid, not legal advice. Do not impersonate the user. Do not contact authorities on the user behalf.',
    escalations: [
      { name: 'Welcome Desk (Stadt Zürich)', url: 'https://www.stadt-zuerich.ch/de/lebenslagen/neu-in-zuerich/zuzug-ausland/welcome-desk.html', description: 'Free information for newcomers, multilingual' },
      { name: 'MIRSAH (SAH Zürich)', url: 'https://www.sah-zh.ch/angebot/mirsah/', phone: '+41 44 291 00 15', description: 'Legal counselling for migration and integration law' },
      { name: 'Solinetz Zürich', url: 'https://solinetz-zh.ch/', description: 'Network supporting migrants and refugees' }
    ]
  }
</constants_verbatim>

<requirements_md_edits>
<!-- The exact edits to .planning/REQUIREMENTS.md (D2-07, D2-08) -->

1. SPIKE-01 line (currently line 9). Replace the existing bullet text with:
   "**SPIKE-01**: Confirmed via deployed-origin spike that the Anthropic Messages API accepts direct browser fetch calls from the GitHub Pages production host on desktop Chrome, with the anthropic-dangerous-direct-browser-access: true header (per Phase 0 D-01, D-02, D-05). The earlier wording referencing Gemini, OpenAI, and iOS Safari is superseded."
   Status checkbox remains [x] (Phase 0 passed).

2. KEY-04 bullet (currently line 16). Delete the entire bullet line: "- [ ] **KEY-04**: User can switch between Gemini and OpenAI as their provider; Gemini is the default".

3. Traceability table: delete the KEY-04 row (the line "| KEY-04 | Phase 1 | Pending |"). Do not renumber anything.

4. The line "Every active v1 requirement maps to exactly one phase. 30 of 30 mapped, 0 orphans." -> change "30 of 30" to "29 of 29".
</requirements_md_edits>

<roadmap_md_edits>
<!-- The exact edits to .planning/ROADMAP.md (D2-09) -->

1. Phase 1 "Requirements:" line (currently line 54). Change from "KEY-01, KEY-02, KEY-03, KEY-04, TRUST-04" to "KEY-01, KEY-02, KEY-03, TRUST-04".

2. Phase 1 Success Criteria, criterion 2 (currently line 57). Replace with:
   "2. Provider is hardcoded to Anthropic per Phase 0 outcome (D-01); the UI clearly states that the saved key is an Anthropic API key, so the user knows what their key is for. (Replaces the original Gemini/OpenAI toggle criterion; KEY-04 obsolete.)"

3. Phase 1 "Plans:" line (currently line 61). Change from "TBD" to "2 plans". Add two bullets directly under (mirroring the Phase 0 plan-bullet style):
   - [ ] 01-01-PLAN.md - Skeleton, CSP, appState/setState/render, BYO key flow (paste/save/mask/clear/replace), mic guardrail script, README
   - [ ] 01-02-PLAN.md - Migrationsamt constants (Block A facts, Block D panic phrases, Hochdeutsch request, Block H footer + escalations), final TRUST-04 grep, REQUIREMENTS.md + ROADMAP.md housekeeping

4. Coverage table, Phase 1 row (currently line 130). Change from "KEY-01, KEY-02, KEY-03, KEY-04, TRUST-04" to "KEY-01, KEY-02, KEY-03, TRUST-04".

5. Phase 0 description bullet (line 27): leave intact; the Notes on Phase Boundaries already cover this. No change required.

Do NOT renumber or restructure other sections. Leave Phase 2/3/4/5 untouched.
</roadmap_md_edits>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MIGRATIONSAMT, BLOCK_D, HOCHDEUTSCH_REQUEST, BLOCK_H_FOOTER constants to index.html</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/01-skeleton-state-key-constants/01-RESEARCH.md (Section 4 in full: office identity, address, phone, hours, 2026 closures, Block D phrases, Block H links)
    - .planning/phases/01-skeleton-state-key-constants/01-01-PLAN.md (Plan 01's <interfaces> block for the existing script layout)
    - index.html (the file produced by Plan 01, so you know exactly where to insert the constants)
    - CLAUDE.md (no em or en dashes; short focused comments per section; beginner-friendly explanations)
  </read_first>
  <action>
    Open index.html. Locate the inline <script> block (created by Plan 01). At the very top of that script, BEFORE the appState declaration, add four const declarations in this order:

      1. MIGRATIONSAMT (per D2-02 + <constants_verbatim>)
      2. BLOCK_D (per D2-03)
      3. HOCHDEUTSCH_REQUEST (per D2-03)
      4. BLOCK_H_FOOTER (per D2-04)

    Each constant gets a beginner-friendly comment block above it explaining (a) what it is, (b) why it is hardcoded (LLM forbidden to invent contact info / canonical phrases / escalation links), (c) where it will be rendered in Phase 3 (Block A / B / D / H per ROADMAP Cheat Sheet Anatomy).

    Above MIGRATIONSAMT, add a single-line source-verification comment:
      // Sources verified 2026-05-14 against https://www.zh.ch/de/sicherheitsdirektion/migrationsamt.html
      // Re-verify annually and before each pilot launch (researcher Trap 5).

    Inside the closures2026 array, add a comment above the 2026-05-13 entry noting that this reduced-hours day is published per-year and the constant needs to be re-derived for 2027 (per researcher Section 4 note).

    Add two // ASSUMPTION FLAG comments (per D2-06):
      - Above BLOCK_D: "ASSUMPTION (researcher A1): exact German phrasing of these panic phrases is conventional usage. Confirm with a native speaker before Phase 3 renders them; updating these strings is a one-line change per phrase."
      - Above closures2026: "ASSUMPTION (researcher A2): 24 Dec 2026 Migrationsamt schedule is currently treated as normal hours. canton Zürich practice often makes this a half-day; confirm with Francisco or via zh.ch before the Phase 5 pilot. If half-day applies, add { date: '2026-12-24', name: 'Tag vor Weihnachten', type: 'reduced', closesAt: '12:00' } to closures2026."

    Constraints:
    - These constants are TOP-LEVEL within the <script> block. Do NOT nest them inside appState. They are module-level data, not state (D2-05).
    - Do NOT add any rendering code for these constants. No new DOM elements. No setState changes. The UI in Phase 1 stays exactly as Plan 01 shipped it.
    - Use single-quoted strings consistent with the existing code style. UTF-8 special characters (ü, ö, German umlauts) must be preserved exactly. Verify the file is saved as UTF-8 (matches Plan 01).
    - Keep all comments free of em dashes, en dashes, and contractions where formal phrasing matters. Plain ASCII hyphens only in comments.
    - All German strings must include their proper diacritics (Können, Wiederhören, Zürich, etc.). The file already uses UTF-8 (Plan 01 includes <meta charset="UTF-8">).

    Implements: D2-01 through D2-06. Constants placement per ROADMAP "Notes on Phase Boundaries" ("Block A, D, G, H content is hardcoded in Phase 1").
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const checks={hasMigrationsamtConst:/const\\s+MIGRATIONSAMT\\s*=\\s*\\{/.test(h),hasOfficialName:/Migrationsamt des Kantons Z[\\u00fc]rich/.test(h),hasAddress:/Berninastrasse\\s*45/.test(h),hasPostfach:/Postfach/.test(h),hasPostalCode:/8090/.test(h),hasPhoneIntl:/\\+41\\s*43\\s*259\\s*88\\s*00/.test(h),hasPhoneNational:/043\\s*259\\s*88\\s*00/.test(h),hasEmail:/info@ma\\.zh\\.ch/.test(h),hasWebsite:/zh\\.ch\\/de\\/sicherheitsdirektion\\/migrationsamt/.test(h),hasPhoneHoursMorning:/08:00[\\s\\S]{0,80}11:45/.test(h),hasPhoneHoursAfternoon:/13:00[\\s\\S]{0,80}16:30/.test(h),hasCounterHours:/counterHours/.test(h),hasClosures2026:/closures2026/.test(h),hasNeujahrstag:/Neujahrstag/.test(h),hasKarfreitag:/Karfreitag/.test(h),hasOstermontag:/Ostermontag/.test(h),hasAuffahrt:/Auffahrt/.test(h),hasPfingstmontag:/Pfingstmontag/.test(h),hasWeihnachtstag:/Weihnachtstag/.test(h),hasReducedHoursEntry:/'14:30'/.test(h),hasBlockD:/const\\s+BLOCK_D\\s*=\\s*\\[/.test(h),hasBlockDRepeat:/wiederholen/.test(h),hasBlockDSlower:/langsamer/.test(h),hasBlockDEmail:/per E-Mail/.test(h),hasBlockDUnderstood:/Ich habe verstanden/.test(h),hasBlockDThanks:/Vielen Dank/.test(h),hasBlockDGoodbye:/Auf Wiederh[\\u00f6]ren/.test(h),hasHochdeutsch:/const\\s+HOCHDEUTSCH_REQUEST\\s*=\\s*\\{/.test(h)&amp;&amp;/Hochdeutsch/.test(h),hasBlockHFooter:/const\\s+BLOCK_H_FOOTER\\s*=\\s*\\{/.test(h),hasWelcomeDesk:/welcome-desk/.test(h),hasMirsah:/mirsah/i.test(h),hasMirsahPhone:/\\+41\\s*44\\s*291\\s*00\\s*15/.test(h),hasSolinetz:/solinetz-zh\\.ch/.test(h),hasDisclaimer:/preparation aid, not legal advice/i.test(h),hasSourcesVerifiedComment:/Sources verified 2026-05-14/.test(h),hasAssumptionA1:/ASSUMPTION[\\s\\S]{0,40}(A1|native speaker)/.test(h),hasAssumptionA2:/ASSUMPTION[\\s\\S]{0,40}(A2|24 Dec|Migrationsamt schedule)/.test(h),noGetUserMedia:!/getUserMedia/.test(h),noEmDashInConstants:!/\\u2014|\\u2013/.test(h)};console.log(JSON.stringify(checks,null,2));process.exit(Object.values(checks).every(Boolean)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: index.html contains const MIGRATIONSAMT = { ... } with nameDe equal to 'Migrationsamt des Kantons Zürich'
    - Source assertion: the address fields contain Berninastrasse 45, Postfach, 8090, and Zürich
    - Source assertion: the phone fields contain +41 43 259 88 00 and 043 259 88 00
    - Source assertion: email is info@ma.zh.ch
    - Source assertion: phoneHours.mondayToFriday has two windows including 08:00 to 11:45 and 13:00 to 16:30
    - Source assertion: closures2026 contains entries for Neujahrstag, Karfreitag, Ostermontag, Auffahrt, Pfingstmontag, Weihnachtstag, and one entry with closesAt '14:30' (the Tag vor Auffahrt reduced-hours entry)
    - Source assertion: const BLOCK_D = [ ... ] contains seven phrase objects covering wiederholen, langsamer, per E-Mail, Ich verstehe nicht, Ich habe verstanden, Vielen Dank, Auf Wiederhören
    - Source assertion: const HOCHDEUTSCH_REQUEST = { ... } exists and the de string mentions Hochdeutsch
    - Source assertion: const BLOCK_H_FOOTER = { ... } contains a disclaimer string with "preparation aid, not legal advice" and an escalations array with welcome-desk URL, mirsah URL, MIRSAH phone +41 44 291 00 15, and solinetz-zh.ch URL
    - Source assertion: comment "Sources verified 2026-05-14" is present
    - Source assertion: ASSUMPTION A1 (native-speaker review) and ASSUMPTION A2 (24 Dec 2026 schedule) comments are both present
    - Source assertion: zero occurrences of getUserMedia anywhere in index.html
    - Source assertion: no em dash (U+2014) or en dash (U+2013) characters anywhere in index.html
    - The automated verify command above exits 0
    - Manual smoke (executor opens index.html in desktop Chrome): page still loads with zero JS console errors, zero CSP-violation errors. The visible UI is unchanged from Plan 01 (the constants are declarations only).
  </acceptance_criteria>
  <done>index.html includes the four named constants at the top of the script block with verified values from researcher Section 4. The visible UI is unchanged. No CSP or JS errors on load.</done>
</task>

<task type="auto">
  <name>Task 2: Full-repo getUserMedia grep verification (final TRUST-04 enforcement)</name>
  <files></files>
  <read_first>
    - .planning/phases/01-skeleton-state-key-constants/01-RESEARCH.md (Section 5: grep enforcement)
    - .planning/REQUIREMENTS.md (TRUST-04 wording)
    - verify-no-mic.ps1 (committed in Plan 01)
  </read_first>
  <action>
    Run two independent verifications and confirm both produce zero matches. Document the result in the SUMMARY for this plan.

    1. Run the script from Plan 01:
         powershell -File .\verify-no-mic.ps1
       Expected output: "verify-no-mic: no matches. OK." and exit code 0.

    2. Run a broader whole-repo PowerShell scan (belt-and-suspenders, per D2-10):
         Get-ChildItem -Path . -Recurse -Include *.html,*.js,*.ps1,*.css,*.md `
           | Where-Object { $_.FullName -notlike "*\.git\*" -and $_.FullName -notlike "*\.planning\*" } `
           | Select-String -Pattern "getUserMedia" -SimpleMatch
       Expected output: empty (zero matches). The Get-ChildItem filters out .git/ (irrelevant) and .planning/ (where the term legitimately appears as documentation in researcher Section 5 and in this plan file).

    If either check produces a match, treat it as a TRUST-04 violation, stop, and surface the file/line to Francisco before continuing.

    Do not create new files. Do not modify existing files. This is a verification-only task.

    Implements: TRUST-04 final enforcement (D2-10).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ok = $true; try { &amp; .\verify-no-mic.ps1; if ($LASTEXITCODE -ne 0) { $ok = $false } } catch { $ok = $false }; $matches = Get-ChildItem -Path . -Recurse -Include *.html,*.js,*.ps1,*.css,*.md -ErrorAction SilentlyContinue | Where-Object { $_.FullName -notlike '*\.git\*' -and $_.FullName -notlike '*\.planning\*' } | Select-String -Pattern 'getUserMedia' -SimpleMatch; if ($matches) { $ok = $false; $matches | ForEach-Object { Write-Host $_.Path ':' $_.LineNumber ':' $_.Line } }; if ($ok) { Write-Host 'TRUST-04 OK: zero matches in source.'; exit 0 } else { Write-Host 'TRUST-04 FAIL.'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - verify-no-mic.ps1 exits 0 with "verify-no-mic: no matches. OK."
    - The whole-repo Select-String (excluding .git/ and .planning/) returns zero matches for getUserMedia
    - The automated verify command above exits 0
    - No new files created, no existing files modified
  </acceptance_criteria>
  <done>Both grep checks return zero matches. TRUST-04 is fully enforced for Phase 1's source surface.</done>
</task>

<task type="auto">
  <name>Task 3: Housekeeping - amend REQUIREMENTS.md and ROADMAP.md (drop KEY-04, rewrite SPIKE-01, sync Phase 1 entry)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current text, especially lines 9, 16, and the Traceability table)
    - .planning/ROADMAP.md (current text, especially the Phase 0 description, the Phase 1 details block at lines 51-62, the Coverage table at lines 123-134, and the Notes on Phase Boundaries)
    - .planning/phases/00-cors-and-provider-spike/00-CONTEXT.md (D-01, D-02, D-05 wording for the new SPIKE-01 text)
    - .planning/phases/00-cors-and-provider-spike/00-01-SUMMARY.md (architecture commit language for Phase 1)
  </read_first>
  <action>
    Apply the exact edits described in <requirements_md_edits> and <roadmap_md_edits> above. Do not touch any other lines in either file. Do not reorder or renumber. Use Edit operations (one per logical change) so the diffs are inspectable.

    REQUIREMENTS.md edits (D2-07, D2-08):
    1. Replace the SPIKE-01 bullet (line ~9) with the new wording from <requirements_md_edits> item 1. Keep status [x] (Phase 0 passed).
    2. Delete the KEY-04 bullet (line ~16) entirely. Do not leave a blank or commented-out placeholder.
    3. In the Traceability table, delete the "| KEY-04 | Phase 1 | Pending |" row.
    4. Change "Every active v1 requirement maps to exactly one phase. 30 of 30 mapped, 0 orphans." to "Every active v1 requirement maps to exactly one phase. 29 of 29 mapped, 0 orphans."

    ROADMAP.md edits (D2-09):
    1. In the Phase 1 details block, change "**Requirements**: KEY-01, KEY-02, KEY-03, KEY-04, TRUST-04" to "**Requirements**: KEY-01, KEY-02, KEY-03, TRUST-04".
    2. In the Phase 1 Success Criteria, replace criterion 2 with the new Anthropic-hardcoded criterion from <roadmap_md_edits> item 2.
    3. Change "**Plans**: TBD" (Phase 1) to "**Plans**: 2 plans" and add two bullets directly below (matching the Phase 0 style of one bullet per plan):
       - [ ] 01-01-PLAN.md - Skeleton, CSP, appState/setState/render, BYO key flow (paste/save/mask/clear/replace), mic guardrail script, README
       - [ ] 01-02-PLAN.md - Migrationsamt constants (Block A facts, Block D panic phrases, Hochdeutsch request, Block H footer + escalations), final TRUST-04 grep, REQUIREMENTS.md + ROADMAP.md housekeeping
    4. In the Coverage table, change the Phase 1 row's Requirements column from "KEY-01, KEY-02, KEY-03, KEY-04, TRUST-04" to "KEY-01, KEY-02, KEY-03, TRUST-04".
    5. Leave the existing Phase 0 description (line 27) and the Notes on Phase Boundaries untouched; they already include the supersession language.

    No em or en dashes in any new copy (CLAUDE.md).

    Implements: D2-07, D2-08, D2-09. Closes the open todo in STATE.md "Open Todos" line about dropping KEY-04 and amending SPIKE-01.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const rm=fs.readFileSync('.planning/ROADMAP.md','utf8');const checks={reqHasNoKey04Bullet:!/\\*\\*KEY-04\\*\\*/.test(req),reqHasNoKey04Row:!/\\|\\s*KEY-04\\s*\\|\\s*Phase\\s*1\\s*\\|/.test(req),reqOrphanCountUpdated:/29 of 29/.test(req)&amp;&amp;!/30 of 30/.test(req),reqSpike01Anthropic:/SPIKE-01[\\s\\S]{0,400}Anthropic/.test(req),reqSpike01NoGemini:!/SPIKE-01[\\s\\S]{0,400}Gemini/.test(req),reqSpike01NoOpenAI:!/SPIKE-01[\\s\\S]{0,400}OpenAI/.test(req),reqSpike01NoIosSafari:!/SPIKE-01[\\s\\S]{0,400}iOS/.test(req),reqSpike01Header:/SPIKE-01[\\s\\S]{0,400}anthropic-dangerous-direct-browser-access/.test(req),reqKey01Present:/\\*\\*KEY-01\\*\\*/.test(req),reqKey02Present:/\\*\\*KEY-02\\*\\*/.test(req),reqKey03Present:/\\*\\*KEY-03\\*\\*/.test(req),reqTrust04Present:/\\*\\*TRUST-04\\*\\*/.test(req),rmPhase1RequirementsClean:/Phase 1[\\s\\S]{0,2000}\\*\\*Requirements\\*\\*:\\s*KEY-01,\\s*KEY-02,\\s*KEY-03,\\s*TRUST-04(?!,\\s*KEY-04)/.test(rm),rmPhase1NoKey04:!/Phase 1[\\s\\S]{0,800}KEY-04/.test(rm),rmPhase1Plans2:/Phase 1[\\s\\S]{0,2000}\\*\\*Plans\\*\\*:\\s*2 plans/.test(rm),rmPhase1Plan01Listed:/01-01-PLAN\\.md/.test(rm),rmPhase1Plan02Listed:/01-02-PLAN\\.md/.test(rm),rmPhase1CriterionAnthropic:/Phase 1[\\s\\S]{0,3000}hardcoded to Anthropic/.test(rm),rmCoverageTablePhase1Clean:/\\|\\s*1\\s*\\|\\s*KEY-01,\\s*KEY-02,\\s*KEY-03,\\s*TRUST-04\\s*\\|/.test(rm),rmNoEmDash:!/[\\u2014\\u2013]/.test(rm),reqNoEmDash:!/[\\u2014\\u2013]/.test(req)};console.log(JSON.stringify(checks,null,2));process.exit(Object.values(checks).every(Boolean)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md no longer contains a "**KEY-04**" bullet anywhere
    - REQUIREMENTS.md Traceability table no longer contains a KEY-04 row
    - REQUIREMENTS.md contains "29 of 29 mapped, 0 orphans" and no longer contains "30 of 30"
    - REQUIREMENTS.md SPIKE-01 bullet mentions Anthropic and the dangerous-direct-browser-access header; no longer mentions Gemini, OpenAI, or iOS Safari
    - REQUIREMENTS.md still lists KEY-01, KEY-02, KEY-03, TRUST-04 as Phase 1 requirements
    - ROADMAP.md Phase 1 Requirements line reads "KEY-01, KEY-02, KEY-03, TRUST-04" (KEY-04 removed)
    - ROADMAP.md Phase 1 Plans line reads "2 plans" with bullets pointing to 01-01-PLAN.md and 01-02-PLAN.md
    - ROADMAP.md Phase 1 Success Criterion 2 mentions "hardcoded to Anthropic"
    - ROADMAP.md Coverage table Phase 1 row reads exactly "KEY-01, KEY-02, KEY-03, TRUST-04"
    - Neither file contains em dashes (U+2014) or en dashes (U+2013)
    - The automated verify command above exits 0
    - git diff inspection confirms ONLY the edits above were made; no other lines changed
  </acceptance_criteria>
  <done>REQUIREMENTS.md and ROADMAP.md are aligned with the Phase 0 outcome and the new Phase 1 plan structure. The STATE.md "Open Todos" item about dropping KEY-04 and amending SPIKE-01 is now closeable.</done>
</task>

</tasks>

<verification>
End-of-plan checks:
- index.html now contains MIGRATIONSAMT, BLOCK_D, HOCHDEUTSCH_REQUEST, BLOCK_H_FOOTER as top-of-script consts. UI is unchanged.
- The hardcoded Migrationsamt phone, address, hours, and 2026 closures are present and verifiable against researcher Section 4.
- TRUST-04 is fully enforced: verify-no-mic.ps1 exits 0; whole-repo PowerShell Select-String for getUserMedia returns zero matches.
- REQUIREMENTS.md no longer references KEY-04 or the Gemini/OpenAI/iOS-Safari wording for SPIKE-01.
- ROADMAP.md Phase 1 Requirements, Success Criteria, Plans, and Coverage table all match the new Anthropic-only structure.
- spike/ folder is untouched.
</verification>

<success_criteria>
- Phase 1 fully satisfies KEY-01, KEY-02, KEY-03 (Plan 01) and TRUST-04 (Plan 01 CSP + Plan 02 final grep) per ROADMAP coverage.
- Every fact the LLM is permanently forbidden from generating (office phone, address, hours, 2026 closures, panic phrases, Hochdeutsch request line, footer disclaimer, escalation URLs/phone) is now hardcoded in index.html and available to Phase 3 rendering.
- The Phase 0 outcome (Anthropic-only, desktop Chrome only) is correctly reflected in REQUIREMENTS.md and ROADMAP.md; no stale Gemini/OpenAI/iOS-Safari language remains in the active requirement set for v1.
- Phase 2 can start with no doc cleanup blockers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-state-key-constants/01-02-SUMMARY.md` capturing:
- The exact final shape of MIGRATIONSAMT, BLOCK_D, HOCHDEUTSCH_REQUEST, BLOCK_H_FOOTER (key fields, not full bodies)
- Confirmation that both grep checks (verify-no-mic.ps1 and the whole-repo Select-String) returned zero matches
- The exact diffs applied to REQUIREMENTS.md and ROADMAP.md (logical, not byte-level)
- Any deviations from researcher Section 4 wording (e.g. if Francisco asked for a different Block D phrasing)
- Flags for Phase 3 and Phase 5: researcher assumptions A1 (Block D native-speaker review) and A2 (24 Dec 2026 schedule) still pending Francisco confirmation
</output>
