---
phase: 00-cors-and-provider-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/index.html
  - .planning/PROJECT.md
autonomous: false
requirements:
  - SPIKE-01
must_haves:
  truths:
    - "A static HTML page at spike/index.html exists with one button, one text input for the API key, and a result area"
    - "Clicking the button issues a fetch to https://api.anthropic.com/v1/messages with the header anthropic-dangerous-direct-browser-access: true and a small Migrationsamt-shaped prompt"
    - "Developer can open the GitHub Pages production URL https://pacomc999.github.io/<repo>/spike/ in desktop Chrome and receive a non-empty German completion"
    - "If CORS or a browser-origin 4xx blocks the call, the failure mode is visible in the result area (not silent)"
    - "The API key is entered into a text/password field at runtime and is never hardcoded in source"
    - "No third-party CDN scripts are loaded by the spike page"
    - "PROJECT.md Key Decisions section contains a dated Phase 0 outcome entry capturing what was tested, the result, and the architecture commit for Phase 1"
  artifacts:
    - path: "spike/index.html"
      provides: "Single-file vanilla HTML spike page with inline <style> and <script> that posts a hardcoded Migrationsamt-shaped prompt to api.anthropic.com/v1/messages and renders the completion"
      contains: "anthropic-dangerous-direct-browser-access"
    - path: ".planning/PROJECT.md"
      provides: "Phase 0 outcome note under Key Decisions"
      contains: "Phase 0"
  key_links:
    - from: "spike/index.html"
      to: "https://api.anthropic.com/v1/messages"
      via: "fetch with x-api-key, anthropic-version, anthropic-dangerous-direct-browser-access headers"
      pattern: "fetch.*api\\.anthropic\\.com/v1/messages"
    - from: "GitHub Pages site settings"
      to: "spike/ folder on the default branch"
      via: "Pages enabled in repo settings, source set to branch + root, build completes before manual test"
      pattern: "https://pacomc999\\.github\\.io/.*/spike/"
---

<objective>
Run the Phase 0 spike: build a minimal single-file HTML page that calls the Anthropic Messages API directly from desktop Chrome, deploy it via GitHub Pages, verify it returns sensible German for a Migrationsamt-shaped prompt, and record the outcome in PROJECT.md Key Decisions.

Purpose: De-risk the load-bearing assumption that v1 can be a pure browser app with no backend. The yes/no answer from this spike determines whether Phase 1 builds against direct Anthropic fetch (D-01) or pivots to Plan B (Cloudflare Workers proxy or provider swap, decided at the moment of failure per D-13).

Output: spike/index.html committed and deployed; PROJECT.md updated with the Phase 0 outcome note (per D-14).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-cors-and-provider-spike/00-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Anthropic Messages API shape the spike must hit. Vanilla fetch only; no SDK. -->
<!-- Endpoint: POST https://api.anthropic.com/v1/messages -->
<!-- Required request headers (per D-03 and Anthropic browser policy): -->
<!--   x-api-key: <user-entered key, never hardcoded> -->
<!--   anthropic-version: 2023-06-01 -->
<!--   anthropic-dangerous-direct-browser-access: true -->
<!--   content-type: application/json -->
<!-- Required request body fields: -->
<!--   model: a current Anthropic model id (planner suggests "claude-3-5-haiku-latest" for low cost; executor may substitute a current valid model id if that one is deprecated) -->
<!--   max_tokens: small integer, e.g. 200 -->
<!--   messages: [{ role: "user", content: "<Migrationsamt-shaped prompt>" }] -->
<!-- Successful response shape: -->
<!--   { content: [{ type: "text", text: "<German completion>" }], ... } -->
<!--   Extract content[0].text for display. -->
<!-- Failure modes the result area must surface verbatim: -->
<!--   - Network/CORS error from fetch (caught as exception) -->
<!--   - Non-2xx response (display status code and response body text) -->
</interfaces>

<spike_prompt>
<!-- Suggested Migrationsamt-shaped prompt text for the spike (Claude's Discretion per CONTEXT.md). -->
<!-- Hardcoded in spike/index.html. Tests both CORS and German bureaucratic vocabulary quality. -->
User message content: "Translate the following into formal Standard German suitable for a phone call to the Migrationsamt of canton Zürich. Reply with only the German translation, no explanation. Source: I would like to renew my B residence permit. My case reference number is ZH-12345."
</spike_prompt>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Anthropic CORS spike page</name>
  <files>spike/index.html</files>
  <read_first>
    - .planning/phases/00-cors-and-provider-spike/00-CONTEXT.md (decisions D-01 through D-16)
    - CLAUDE.md (vanilla HTML/CSS/JS only, no frameworks, no TypeScript, beginner-friendly comments)
    - C:\Users\pacoe\coding_projects\CLAUDE.md (single-file inline-style/script pattern from sibling projects)
  </read_first>
  <action>
    Create spike/index.html as a single self-contained file with inline &lt;style&gt; and &lt;script&gt;. No external scripts, no CDN imports, no build step (enforces D-16).

    UI surface (keep it ugly and minimal, this is throwaway):
    - One &lt;input&gt; with type="password" for the Anthropic API key, plus a label. The key MUST NOT be hardcoded (D-15).
    - One &lt;button&gt; labeled "Run spike" that triggers the fetch.
    - One &lt;pre&gt; or &lt;div&gt; result area where the German completion is rendered via textContent (never innerHTML on response data).
    - One status line that shows "idle / running / success / failed: &lt;reason&gt;".

    Behavior on button click:
    - Read the key from the input. If empty, show an inline message and abort.
    - POST to https://api.anthropic.com/v1/messages with headers x-api-key (from input), anthropic-version: 2023-06-01, anthropic-dangerous-direct-browser-access: true, content-type: application/json. The dangerous-direct-browser-access header is mandatory per D-03.
    - Request body uses model "claude-3-5-haiku-latest" (or a current valid Anthropic model id if that is deprecated at execution time), max_tokens 200, and a single user message containing the Migrationsamt-shaped prompt from &lt;spike_prompt&gt; above.
    - On success: extract content[0].text and render it in the result area via textContent. Status line shows "success".
    - On non-2xx: render status code and response body text in the result area. Status line shows "failed: HTTP &lt;code&gt;".
    - On fetch exception (likely CORS): render the error message in the result area. Status line shows "failed: network or CORS".

    Privacy constraints (D-15, D-16):
    - Do NOT console.log the API key.
    - Do NOT console.log the full response body if it could contain the key (responses do not echo the key, so the response itself is fine, but do not log the request headers object).
    - Do NOT import marked.js, DOMPurify, or any other CDN script. Pure vanilla.
    - All response text goes to the DOM via textContent, never innerHTML.

    Code style per CLAUDE.md: short focused functions, clear variable names, brief comments explaining each section. No em dashes in visible text.

    Implements decisions: D-01 (Anthropic only), D-02 (Anthropic-only spike), D-03 (header included), D-15 (key field, not hardcoded), D-16 (no CDN), and the Claude's Discretion choices (spike folder = spike/, filename = index.html, hardcoded model, hardcoded Migrationsamt-shaped prompt).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('spike/index.html','utf8');const checks={hasDangerousHeader:/anthropic-dangerous-direct-browser-access/.test(s),hasMessagesEndpoint:/api\.anthropic\.com\/v1\/messages/.test(s),hasPasswordInput:/type=[\"']password[\"']/.test(s),noCdnScript:!/&lt;script[^&gt;]+src=[\"']https?:/i.test(s),noConsoleLogKey:!/console\.log\([^)]*apiKey/i.test(s),hasAnthropicVersion:/anthropic-version/.test(s),hasMigrationsamtPrompt:/Migrationsamt/i.test(s)};console.log(JSON.stringify(checks,null,2));process.exit(Object.values(checks).every(Boolean)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: spike/index.html exists and contains the literal string anthropic-dangerous-direct-browser-access
    - Source assertion: spike/index.html contains a POST target of api.anthropic.com/v1/messages
    - Source assertion: the API key input has type="password"
    - Source assertion: no &lt;script src="https://..."&gt; or src="http://..." tag is present (no CDN scripts)
    - Source assertion: the prompt string mentions Migrationsamt and a permit/case reference (not a "hello world" prompt)
    - Source assertion: the header anthropic-version is set
    - Source assertion: there is no console.log call that prints the apiKey variable
    - The automated verify command above exits 0
  </acceptance_criteria>
  <done>spike/index.html is committed, opens cleanly in Chrome from the local filesystem (no JS console errors on load), and all source assertions in acceptance_criteria pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to GitHub Pages and run the real CORS test (human verification)</name>
  <what-built>
    spike/index.html, a single-file HTML page that calls Anthropic's Messages API directly from the browser with the dangerous-direct-browser-access header, a Migrationsamt-shaped German translation prompt, and a result area.

    Before this checkpoint, Claude has:
    - Committed spike/index.html to the default branch and pushed to origin (pacomc999/&lt;repo&gt;) so it is visible on GitHub.
    - Attempted to enable GitHub Pages via `gh` CLI if not already enabled, pointing the source at the default branch root (the spike lives at /spike/). If `gh` cannot enable Pages non-interactively on this account, Claude has surfaced the exact manual step below.
  </what-built>
  <how-to-verify>
    Manual steps the developer (Francisco) performs in desktop Chrome:

    1. Confirm GitHub Pages is enabled for this repo. In a browser, open:
       https://github.com/pacomc999/&lt;repo-name&gt;/settings/pages
       If Pages is not enabled, set Source = Deploy from a branch, Branch = main (or whatever the default branch is), Folder = / (root), and click Save. Wait for the green "Your site is live at ..." banner. This step is one-time per repo.

    2. Wait for the Pages build to finish. The Actions tab of the repo shows a "pages-build-deployment" workflow. Confirm it is green before proceeding. First builds typically take 30 to 90 seconds.

    3. Open the deployed URL in desktop Chrome (not Edge, not Firefox, per D-05):
       https://pacomc999.github.io/&lt;repo-name&gt;/spike/

    4. Open Chrome DevTools (F12), Network tab, before running the spike. This will let you confirm whether a CORS preflight or response error occurred.

    5. Paste the real Anthropic API key into the password field. Click "Run spike".

    6. Observe the result area. Expected outcome (spike PASSES):
       - Status line transitions to "success".
       - Result area contains non-empty German text that reads like a sensible translation of "I would like to renew my B residence permit. My case reference number is ZH-12345." For example, something close to: "Ich möchte meinen B-Ausweis verlängern. Meine Fallreferenznummer lautet ZH-12345." Exact wording will vary; the point is that it is German and on-topic (per D-09, D-10, D-12).
       - DevTools Network tab shows a 200 response from api.anthropic.com.

    7. Failure outcomes (spike FAILS, triggers D-13 Plan B decision):
       - DevTools shows a CORS error in the console, or the preflight OPTIONS request fails.
       - The /v1/messages response is 4xx with a body that mentions browser-origin or direct-browser-access restrictions.
       - Status line shows "failed: network or CORS" or "failed: HTTP 4xx".

    8. Either way, copy the exact result area text and status line verbatim into the resume signal so Task 3 can record the outcome accurately.

    Notes:
    - If Pages is not yet enabled and you do not want to enable it via the web UI, tell Claude and Claude will attempt `gh api -X PUT repos/pacomc999/&lt;repo&gt;/pages -f source[branch]=main -f source[path]=/`. This requires the `gh` CLI to be authenticated.
    - The spike is intentionally desktop Chrome only (D-05). Do NOT test iOS Safari.
    - Do NOT commit the API key. It only ever lives in the runtime password field.
  </how-to-verify>
  <resume-signal>
    Respond with one of:
    - "pass: &lt;paste the German completion verbatim&gt;"
    - "fail: &lt;paste the status line and any error from the result area or DevTools&gt;"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Phase 0 outcome in PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (Key Decisions section at line 54)
    - .planning/phases/00-cors-and-provider-spike/00-CONTEXT.md (D-14 wording on the decision-moment note)
    - The resume-signal text from Task 2 (pass or fail, plus verbatim result)
  </read_first>
  <action>
    Append a new dated entry to the "## Key Decisions" section of .planning/PROJECT.md (do not rewrite existing entries, do not reorder).

    The entry must capture (per D-14):
    - Date in ISO form (today's date, 2026-05-14).
    - What was tested: Anthropic Messages API directly from a GitHub Pages-hosted static HTML page on desktop Chrome, with the anthropic-dangerous-direct-browser-access: true header, using a small Migrationsamt-shaped translation prompt.
    - The result, taken from the Task 2 resume signal: either "pass" with a one-line summary of the German completion received, or "fail" with the observed failure mode (CORS preflight blocked, HTTP 4xx with browser-origin rejection, etc.).
    - The architecture commit for Phase 1:
      - If pass: Phase 1 builds against direct browser fetch to Anthropic, per D-01. KEY-04 (provider toggle) is now obsolete and will be removed from REQUIREMENTS.md when Phase 1 is planned.
      - If fail: defer the architecture commit to a follow-up decision moment between (a) Cloudflare Workers proxy with BYOK pass-through or (b) pivot v1 to Gemini per D-13. Do not pre-commit; surface that Phase 1 planning is blocked until the developer chooses.
    - A short note that scope was deliberately Anthropic-only, desktop-Chrome-only (D-02, D-05), so this result does not say anything about Gemini, OpenAI, or iOS Safari.

    Format: a single new H3 subsection inside Key Decisions, e.g. "### 2026-05-14: Phase 0 CORS spike outcome (Anthropic, desktop Chrome)". Use plain prose paragraphs and a short bullet list for the architecture-commit options. No em dashes in visible text (CLAUDE.md preference).

    Implements: D-14 (decision-moment note must be recorded regardless of pass or fail).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/PROJECT.md','utf8');const ks=s.split('## Key Decisions')[1]||'';const checks={hasKeyDecisionsSection:/## Key Decisions/.test(s),hasPhase0Heading:/Phase 0/i.test(ks)&amp;&amp;/spike/i.test(ks),hasDate2026:/2026-05-14/.test(ks),hasAnthropicMention:/Anthropic/i.test(ks),hasChromeMention:/Chrome/i.test(ks),hasOutcomeWord:/(pass|fail|success|blocked)/i.test(ks)};console.log(JSON.stringify(checks,null,2));process.exit(Object.values(checks).every(Boolean)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: .planning/PROJECT.md still contains "## Key Decisions"
    - Source assertion: a new subsection inside Key Decisions mentions "Phase 0" and "spike"
    - Source assertion: the new entry contains the date 2026-05-14
    - Source assertion: the entry names Anthropic and Chrome explicitly (so the scope of the result is unambiguous)
    - Source assertion: the entry contains one of the words pass, fail, success, or blocked (the outcome is stated, not implied)
    - Source assertion: pre-existing content above "## Key Decisions" is unchanged (no accidental rewrite). Reviewer can confirm with `git diff .planning/PROJECT.md` showing only additions inside or after the Key Decisions heading.
    - The automated verify command above exits 0
  </acceptance_criteria>
  <done>.planning/PROJECT.md has a new dated Phase 0 outcome entry under Key Decisions that captures what was tested, the result from Task 2, and the architecture commit (or deferral) for Phase 1, per D-14.</done>
</task>

</tasks>

<verification>
End-of-phase checks:
- spike/index.html exists, is single-file vanilla HTML, contains the dangerous-direct-browser-access header, and has no third-party CDN scripts
- The spike has been deployed to https://pacomc999.github.io/&lt;repo&gt;/spike/ and the developer has tested it from desktop Chrome with a real Anthropic key
- The test outcome (pass or fail) is recorded verbatim in .planning/PROJECT.md Key Decisions with date 2026-05-14
- SPIKE-01 is now resolvable: either "Anthropic direct browser fetch confirmed working from production origin on desktop Chrome" (pass) or "Plan B decision required, Phase 1 blocked pending developer choice" (fail). Note: the SPIKE-01 wording in REQUIREMENTS.md still references Gemini + OpenAI + iOS Safari; that wording is superseded by D-01, D-02, D-05 and will be amended in Phase 1 planning.
</verification>

<success_criteria>
- The yes/no question "can a GitHub-Pages-hosted static page call Anthropic Messages directly from desktop Chrome with a sensible German response?" has been answered with real evidence
- The answer is recorded in PROJECT.md Key Decisions so Phase 1 planning can reference it
- No v1 code, no Gemini code, no OpenAI code, no iOS testing was undertaken (scope respected per D-01, D-02, D-05)
- No API key was committed to the repo (D-15)
- No third-party CDN script was added (D-16)
</success_criteria>

<output>
After completion, create `.planning/phases/00-cors-and-provider-spike/00-01-SUMMARY.md` capturing:
- The exact prompt used
- The exact response received (German text) or the exact failure
- The Anthropic model id actually used at execution time
- Confirmation of whether GitHub Pages was already enabled or had to be enabled during this phase
- The architecture commit for Phase 1 (direct Anthropic, or "Plan B decision pending")
</output>
